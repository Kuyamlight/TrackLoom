# TrackLoom 项目协作约定

本文件记录 TrackLoom（织音）长期有效的项目目标、工程原则和协作要求。所有参与者和自动化代理在修改本仓库时均应遵守。

## 1. 项目信息

- 名称：TrackLoom｜织音
- 定位：开源、Windows 优先、AI 原生音乐创作工作站
- 主要语言：C++20；AI 服务使用 Python
- 桌面与音频框架：JUCE
- 构建系统：CMake
- 计划许可证：AGPL-3.0
- 目标受众：专业音乐人、音乐创作者、对音乐创作感兴趣但缺少乐理基础的用户
- 需求与规划唯一基线：`docs/TrackLoom-最终需求与规划.md`

## 2. 输出与沟通要求

- 面向用户的说明、规划、文档和主要注释默认使用简体中文。
- 代码标识符、公开 API、文件名和行业标准术语优先使用清晰的英文。
- 回答先给结论，再说明必要依据和下一步。
- 不隐藏技术风险，不把设想表述为已经实现或已经验证。
- 文档应信息完整但避免重复；需求变化后同步更新对应文档。

## 3. 产品目标

TrackLoom 应支持：

- 外设演奏、录音、循环编曲、音频导入和 AI 生成等多种创作入口；
- 可编辑的多轨音频、MIDI、插件、自动化和工程结构；
- 特色循环编曲器及未来其他快速编曲方式；
- 单音色转录、完整音乐分离与 AI 辅助扒谱；
- AI 生成、修改、润色和调度工程工具；
- VST3 乐器、采样器、工具、效果器和分析器；
- 无 AI 配置时仍可使用基础 DAW 功能；
- 工程文件夹与可分享的单文件工程包；
- 标准 MIDI、音频 stems 和开放工程交换格式的导入导出；
- 按项目分类、可管理的 AI 对话和生成内容；
- 不依赖官方服务器的用户存储同步、备份导出与数据恢复。

## 4. 核心工作原则

1. **稳定核心优先**

   高级功能可以提前开发，但不得破坏工程打开、保存、播放和编辑。

2. **非破坏性编辑**

   保留原始素材。合并、冻结、AI 补全和生成默认创建可恢复结果。

3. **统一工程命令**

   用户和 AI 通过同一命令系统修改工程。命令必须可验证、可撤销、可记录。

4. **实时线程安全**

   音频线程不得执行网络请求、AI 推理、阻塞磁盘操作、不可控锁等待或其他高延迟任务。

5. **模块可替换**

   核心不得依赖具体 AI 供应商、模型或插件格式。外部能力通过版本化接口和适配器接入。

6. **来源透明**

   AI 识别、AI 修复和 AI 生成内容必须分别标记，生成内容不得冒充可靠扒谱。

7. **隐私优先级明确**

   云端 AI 调用、用户存储同步、离线备份和 AI 本地访问是不同的数据操作，必须分别授权。工程、轨道和素材的显式禁止规则必须覆盖默认值和 AI 指令。

8. **长期兼容**

   工程格式必须版本化。数据结构变更需要迁移方案、向后兼容考虑和测试。

   TrackLoom 自有工程格式是权威保存格式，不承诺直接读写主流 DAW 的私有原生工程文件。对外互通通过标准 MIDI、音频、stems、DAWproject、AAF/OMF 等导入导出适配器实现，并必须提示无法保留的信息。

9. **故障隔离**

   AI、网络、同步和第三方插件失败不得导致基础 DAW 不可用。VST3 扫描从首次实现起使用辅助进程；稳定版本中的第三方插件通过独立宿主进程运行。

10. **证据后完成**

    声称功能完成、问题修复或测试通过前，必须运行与风险相称的验证并记录结果。

11. **分阶段交付**

    总体规划中的长期能力可以逐步实现。每个阶段必须有明确边界、量化验收指标和可运行成果，不得因远期功能尚未完成而破坏稳定核心。

## 5. 架构边界

- C++/JUCE 主程序负责界面、工程、实时音频、MIDI、循环编曲和任务调度。
- Python 服务负责云端 AI 适配，并为未来本地模型保留接口。
- TrackLoom 不建设官方服务器、官方云盘或 AI 请求中转服务。
- 云端 AI 请求由本地服务直接发送到用户自行配置的第三方 API。
- 数据同步由客户端通过本地同步目录、HTTPS WebDAV 或 SFTP 连接用户指定的存储位置；不支持未加密的传统 FTP 或明文 WebDAV。
- 音频引擎不得直接依赖 AI 服务或具体用户界面。
- 工程系统使用稳定 ID 管理轨道、片段、插件和派生结果。
- 对话和生成内容按项目稳定 ID 分类，但与用户主动保存的工程文件保持独立；只有明确应用的 AI 命令才可修改工程。
- 编曲器通过独立接口接入；循环编曲器是第一种实现，不应写死为唯一形式。
- 循环编曲器持久化音乐时间而非固定采样位置；速度变化、参数生效边界、跨循环 Note Off、重叠和效果尾音必须采用可测试的确定性规则。
- 插件格式通过适配层接入；首期只承诺 VST3。
- 文件夹轨负责分类与折叠；组总线负责音频汇总，两者不得混为同一数据概念。
- 云端 AI 调用、对话同步、未来工程同步和备份恢复必须保持独立模块边界。
- 导入导出适配器不得绕过工程模型和命令验证；导入外部工程交换格式时必须标记来源、缺失信息和可能改变声音的内容。

## 6. 轨道与插件约定

- 普通乐器轨默认使用一个主要音源或采样器，可串联多个 MIDI 工具、效果器和分析器。
- 多音源乐器机架属于后续功能，当前设计不得阻碍未来接入。
- 标准 MIDI 文件只能表达 MIDI 事件和有限元数据，不能保存 VST3 状态、采样器素材、音频效果链、AI 来源信息或 TrackLoom 特有工程历史。
- MIDI 导出默认使用 Standard MIDI File Type 1 保留多轨结构，并提供 Type 0 兼容选项；导出不得默认量化或改写用户演奏时间。
- 轨道的静音、独奏、隐藏、禁用、冻结和隐私权限是独立状态。
- 隐藏只影响显示，不能隐式静音。
- 结构组合、片段整理、渲染合并和共享处理链是不同操作，界面和数据模型不得混为同一“合并”概念。
- 共享处理链除验证插件版本、状态、路由、声道、处理链和自动化外，还必须考虑随机状态、复音分配、非线性处理、侧链、历史缓冲和效果尾音。
- 无法证明声音等价时不得宣称无损，只允许保留独立处理链的结构组合或非破坏性渲染。

## 7. AI 约定

- 通用模型负责理解、规划和调用工具；专业模型负责具体音乐或音频能力。
- 只配置一个模型时，不得假设它具备未声明的能力。
- 多模型选择依据能力、质量、速度、费用、隐私和用户偏好。
- AI 可以调用全部合法工程命令，但不能绕过数据验证、隐私、实时线程和操作历史。
- AI 返回的结构化结果必须验证后才能写入工程。
- AI 异步任务必须携带工程版本和对象前置条件；结果返回时发生相关冲突则重新规划、显示差异或请求确认，不得覆盖用户的新修改。
- 完全代理模式必须支持取消，并限制最大步骤、费用、执行时间和外部数据传输。
- API 密钥不得写入仓库、工程文件、崩溃报告或普通日志。

## 8. 代码与模块质量

- 优先使用现代 C++20、RAII、明确所有权和小型稳定接口。
- 实时音频路径避免动态分配、阻塞锁、异常和不可预测操作。
- 文件和类保持单一职责；出现大型耦合模块时应在相关任务内进行有针对性的拆分。
- 不进行与当前目标无关的大规模重构。
- 外部依赖必须记录版本、来源、许可证和用途。
- 第三方模型、插件和无权分发的素材不得直接加入仓库。
- 实验功能使用功能开关，与稳定路径隔离。

## 9. 测试要求

根据改动范围执行适当测试：

- 工程模型、命令和迁移：单元测试；
- MIDI、循环调度和音频算法：确定性测试和边界测试；
- 播放、录音、导入导出和插件：集成测试；
- 实时音频：延迟、爆音、丢帧和压力测试；
- 保存与恢复：异常关闭、缺失素材、插件崩溃和网络中断测试；
- AI：适配器契约、格式验证、隐私阻断和失败降级测试；
- 数据管理：对话增删归档、生成文件引用、备份恢复、同步冲突、删除传播、断点恢复和损坏检测；
- 用户流程：非乐理用户完成短编曲、修改和导出。

修复缺陷时应优先添加能够复现问题的测试。
阶段性完成标准必须写入对应实施规格，至少包含目标环境、性能预算、连续运行时间、保存恢复条件和用户任务指标。

## 10. 安全与隐私

- 不提交 API 密钥、账号凭据、个人录音或其他敏感素材。
- “云端 AI 调用”只指用户主动调用第三方 AI 服务时，发送完成任务所必需的数据，不代表把完整工程上传到 TrackLoom 服务器。
- 未配置服务或用户未主动发起任务时，不得发生云端 AI 数据传输。
- 云端 AI 调用前检查工程、轨道、素材及其派生依赖的权限，并遵循最小数据原则。
- 禁止发送的内容不得通过缓存、混音、日志或临时导出间接传输。
- 用户存储同步默认关闭；同步凭据存入系统安全凭据，不写入同步数据、备份包或普通日志。
- 对话、生成文件、工程素材和备份具有独立生命周期。生成文件加入工程后必须进入工程素材目录，不能继续依赖对话缓存。
- 删除对话或生成文件前必须检查工程引用；同步删除使用删除标记，避免其他设备恢复已删除内容。
- 备份包必须版本化并带文件清单和校验信息；恢复前允许预览、选择、合并或作为副本恢复。
- 日志只记录诊断所需信息，不记录不必要的完整提示词、原始音频或密钥。
- 外部服务和网页内容是不可信输入，必须验证后使用。

## 11. Git 与文档

- 提交应范围清晰，说明“为什么改”以及用户可见影响。
- 不覆盖或删除用户已有的无关改动。
- 重要架构决定、需求变化和格式变更必须更新文档。
- 需求与规划只维护在 `docs/TrackLoom-最终需求与规划.md`；普通需求变更不得再新建分散的 `docs/superpowers/specs/` 或 `docs/superpowers/plans/` 草稿。
- `AGENTS.md` 只记录协作约定、工程原则和经验证实的长期教训，不承担完整需求规格职责。
- `AGENTS.md` 和 `docs/TrackLoom-最终需求与规划.md` 是长期协作与规划基线，必须纳入 Git 版本控制；临时草稿、构建产物和本机私有设置不得提交。
- 规划文档使用绝对日期，避免“今天”“以后”等歧义表达。
- 尚未实现的功能明确标记为计划或后续目标。

## 12. 用户偏好与已确认决策

- 用户希望长期持续开发，不因毕业时间删除长期功能。
- 增强功能可以尽早开始，但项目必须保持阶段性可运行。
- 首版只支持 Windows；未来平台扩展通过架构预留。
- 首期 AI 以用户自行配置的云端 API 为主。
- 不建设 TrackLoom 官方服务器；云端 AI 由客户端直连用户配置的第三方服务。
- 用户存储同步默认关闭，首期优先支持本地同步目录、HTTPS WebDAV 和 SFTP。
- 默认同步范围是按项目分类的 AI 对话、尚未加入工程的生成内容及相关元数据；工程文件、原始录音和正式工程素材由用户自行保存与备份。
- 未来可以提供独立、可选的工程同步功能，但不得与对话同步隐式捆绑。
- 支持导出版本化备份并选择性恢复对话、生成内容、元数据和非敏感设置。
- 项目完全开源。
- 项目文档和协作沟通默认使用简体中文。
- 循环编曲器是当前唯一确定的特色快速编曲功能，未来允许增加其他形式。
- 开发过程中应积极参考 GitHub 上对本项目有帮助的开源项目、架构和实现经验；参考前必须检查许可证、维护状态和适配成本，不得直接复制不兼容许可证代码或无来源说明的实现。

## 13. 经验教训维护

只记录经过实现、测试或真实故障验证，且对后续开发具有长期价值的经验。不要把临时猜测、一次性命令或未验证方案写入本文件。

新增经验时应包含：

- 触发问题的场景；
- 根本原因；
- 采用的解决方式；
- 后续必须遵守或测试的规则。

当前已确认的长期经验：

- “一个 AI 处理所有任务”不是可靠假设，应采用能力声明和适配器。
- AI 可以拥有广泛编辑权限，但必须通过稳定工程命令执行。
- 文件夹分类和音频总线解决不同问题，数据模型必须分离。
- 固定的循环变体序列可以表达为更长的循环内容，不需要增加冗余抽象。
- 相同插件名称不等于相同音色；比较完整处理状态只是共享处理链的必要条件，无法证明声音等价时不得宣称无损合并。
- Windows/MSVC 构建包含中文注释的 C++ 源码时：
  - 触发场景：首段 C++20 核心库加入简体中文教学注释后，MSVC 19.44 使用默认代码页 936 编译，出现 C4819 警告并引发大量语法错误。
  - 根本原因：源码按 UTF-8 保存，但编译器未按 UTF-8 解析，中文注释字节被误读，破坏后续 C++ 语法解析。
  - 采用的解决方式：在 CMake 中对 MSVC 目标显式添加 `/utf-8` 编译选项，并重新运行构建与 CTest 验证通过。
  - 后续规则：只要源码或注释包含中文，MSVC 构建目标必须保留 UTF-8 编译选项；新增目标时同步设置并纳入构建验证。
- 修改带中文注释的源码时：
  - 触发场景：为轨道混音状态补充实现时，终端按错误代码页显示 UTF-8 中文注释，导致 `apply_patch` 使用乱码注释作为上下文时匹配失败。
  - 根本原因：文件内容是 UTF-8，但 PowerShell 输出采用的显示编码与文件编码不一致；补丁上下文依赖乱码后的注释文本，稳定性低。
  - 采用的解决方式：改用稳定的英文代码行作为补丁上下文；在小型头文件中，必要时直接重写完整文件，保留接口并恢复可读中文注释。
  - 后续规则：修改含中文注释的文件时，补丁上下文优先锚定代码标识符和结构边界，不依赖终端里显示异常的中文注释；改完后必须重新构建和运行 CTest。
- 在 Windows/MSVC Debug 构建中运行会失败的测试时：
  - 触发场景：为音频片段创建、重命名、删除补撤销/重做红测后，`trackloom_app_support_tests.exe` 触发未捕获异常，MSVC Debug CRT 弹出 `abort()` 对话框，CTest 等到超时而不是立刻报告失败。
  - 根本原因：Debug CRT 默认把 abort/assert 报告发到交互弹窗；自动化测试进程被弹窗阻塞，终端拿不到失败结果。
  - 采用的解决方式：在测试入口加入 MSVC 专用 `configureTestFailureOutput()`，用 `_set_abort_behavior`、`_set_error_mode` 和 `_CrtSetReportMode` 把失败输出导向 stderr，并关闭 reportfault 弹窗；自定义 `require()` 在抛异常前先把断言消息写入 stderr，避免测试只返回失败码却没有可读原因。
  - 后续规则：新增 Windows/MSVC 测试可执行入口时，应显式避免 Debug CRT 交互弹窗；红灯测试必须能在 CTest 中快速失败并显示失败原因，不得依赖人工关闭对话框。
- 使用 PowerShell 更新 GitHub PR 正文时：
  - 触发场景：为核心功能 PR 更新 Markdown 正文时，使用 `Set-Content -Encoding UTF8` 写入临时文件后提交给 `gh pr edit --body-file`，PR 正文开头出现不可见 BOM 字符。
  - 根本原因：Windows PowerShell 的 UTF-8 写入方式可能带 BOM；GitHub 将该字节保留在正文开头，导致 Markdown 文本前出现隐藏字符。
  - 采用的解决方式：读取 PR 正文后移除开头 BOM，并使用 `.NET` 的 `System.Text.UTF8Encoding(false)` 生成无 BOM 临时文件重新提交。
  - 后续规则：通过临时文件更新 GitHub Markdown 正文时，应使用无 BOM UTF-8 写入方式；更新后检查正文首行或首字符，确认 `## Summary` 等 Markdown 标题前没有隐藏字符。
- 使用 PowerShell 读取并改写 GitHub PR 正文时：
  - 触发场景：更新 PR #1 正文时，使用 `$body = gh pr view 1 --json body --jq .body` 读取多行 Markdown，再用 `.NET` 写回临时文件后，GitHub PR 正文被压缩成一整行。
  - 根本原因：PowerShell 命令替换会把外部命令多行输出作为数组或字符串处理，传给 `WriteAllText` 时换行被空格化，Markdown 结构丢失。
  - 采用的解决方式：改用明确的 here-string 重写正文，并继续使用 `System.Text.UTF8Encoding(false)` 写入无 BOM 临时文件；更新后用 `gh pr view --json body | ConvertFrom-Json` 检查正文首行、行数和关键条目。
  - 后续规则：需要保留 Markdown 换行时，不要直接把 `gh --jq .body` 的多行输出赋给普通变量后写回；应使用 JSON 对象字段、here-string 或显式 join 保留换行，并在更新后验证正文没有被压成单行。
- 在普通 PowerShell 中运行 MSVC/Ninja 构建时：
  - 触发场景：MIDI 音符核心实现后，直接执行 `cmake --build build`，`cl.exe` 启动但报错找不到 `cstddef`、`cstdint` 等 C++ 标准库头文件。
  - 根本原因：构建目录记录了 MSVC 编译器路径，但当前 shell 没有加载 Visual Studio Build Tools 的 `INCLUDE`、`LIB` 和相关 `PATH` 环境变量。
  - 采用的解决方式：通过 `E:\Android\VS\2022\BuildTools\Common7\Tools\VsDevCmd.bat -arch=x64` 初始化开发环境后，再执行 `cmake --build build`，构建与 CTest 验证通过。
  - 后续规则：在 Windows 上用 MSVC/Ninja 构建 TrackLoom 前，先确认处于 VS Developer Command Prompt 环境，或用 `VsDevCmd.bat` 包裹构建命令；遇到标准库头文件缺失时，先检查编译环境，不要误判为源码问题。
- 实现 MIDI 播放调度时：
  - 触发场景：MIDI 片段已经能保存音符后，需要把工程里的音符转换成可播放事件，并同时遵守静音、禁用、独奏和隐藏等轨道状态。
  - 根本原因：如果直接把 MIDI 调度写进 `AudioEngine`，会过早混合“工程数据转事件”的确定性逻辑和实时音频渲染逻辑，增加测试难度和实时线程风险。
  - 采用的解决方式：新增独立的 `MidiPlayback` 纯调度模块，只读取 `Project`，输出半开 tick 窗口内排序后的 Note On/Note Off 事件，并用 CTest 覆盖窗口边界、排序、静音、禁用、独奏、隐藏和音频片段忽略行为。
  - 后续规则：未来接入 MIDI 设备、插件乐器或采样器时，应优先消费 `MidiPlayback` 的确定性事件输出；设备发送、插件处理和音频渲染桥接要保持独立，不要绕过已测试的调度规则。
- 桥接 `AudioEngine` 渲染结果到 MIDI 输出时：
  - 触发场景：`AudioEngineRenderResult` 已经能产出带 sample offset 的 `scheduledMidiEvents` 后，需要把这些事件交给 `MidiDispatch`，为后续真实设备、插件乐器或采样器输出做入口。
  - 根本原因：如果在 `renderNextBlockWithMidi` 内自动发送 MIDI，接收器失败会和音频渲染成功混在一起，可能让输出层错误影响播放头推进、音频缓冲和渲染返回值。
  - 采用的解决方式：新增 `AudioEngineMidiBridge` 独立薄桥接模块，显式读取 `AudioEngineRenderResult.scheduledMidiEvents` 并复用 `dispatchScheduledMidiEvents`；用 CTest 覆盖正常发送、空结果和接收器失败立即停止。
  - 后续规则：`AudioEngine` 继续只产出渲染结果，不在渲染函数内部打开设备或自动发送 MIDI；真实 MIDI 设备、插件或采样器接入时必须通过显式桥接或等价边界，并保留发送失败不回滚渲染结果的测试。
- 测试 MIDI 半开播放窗口时：
  - 触发场景：为 `PlaybackClock` 验证“前一个 block 不包含右边界 tick，后一个 block 包含左边界 tick”时，测试音符长度设置过短，导致第二个 block 内同时出现 Note On 和 Note Off，CTest 失败。
  - 根本原因：测试目标是边界包含规则，但测试数据额外引入了音符结束事件，混入了与目标无关的行为。
  - 采用的解决方式：把边界测试音符长度延长到第二个 block 的右边界，使该 block 只包含左边界的 Note On，Note Off 正好落在右边界外。
  - 后续规则：写半开窗口测试时，必须让测试数据只触发目标边界事件；如果需要同时验证 Note Off，应单独写测试，避免一个测试同时证明多个时间边界。
- 实现 MIDI 输出消息转换时：
  - 触发场景：新增 `MidiDispatch` 后，自审发现转换函数把未知 `MidiPlaybackEventType` 默认当作 Note Off；补充未知类型测试后 CTest 失败。
  - 根本原因：转换逻辑使用隐式兜底分支，当前 enum 虽然只有 Note On 和 Note Off，但未来扩展 CC、Pitch Bend、Aftertouch 等事件时可能被静默转换成错误的 Note Off。
  - 采用的解决方式：改为显式 `switch` 处理 Note On 和 Note Off，未知类型返回空结果，并让 dispatch 停止发送且报告失败索引。
  - 后续规则：所有 MIDI 事件类型到低层消息的转换必须显式枚举支持范围；新增事件类型时必须先补转换测试，不允许通过默认分支静默降级。
- 实现循环播放窗口切分时：
  - 触发场景：为循环编曲器准备底层播放调度，需要让一个音频 block 在跨过循环右边界时拆成可测试的 tick 子窗口，并保持 MIDI 事件在原始 block 内的 sample offset。
  - 根本原因：如果只返回单个连续 tick 窗口，跨循环播放会错误覆盖循环尾部到循环头部的非连续音乐时间；如果让 `Transport` 或 `AudioEngine` 直接处理回绕，又会过早混合播放头推进、音频渲染和 MIDI 调度。
  - 采用的解决方式：在 `PlaybackClock` 中新增纯函数循环窗口切分，把绝对 sample 位置按循环秒数范围归一化，再按循环右边界拆分半开 tick 子窗口；用 CTest 覆盖跨循环拆分、跨循环 MIDI sample offset、非法循环范围、绝对 sample 归一化和 tempo map 下的循环切分。
  - 后续规则：循环播放、循环编曲器和后续音频引擎回绕接入时，应优先复用 `playbackTickWindowsForLoopedBlock` 和 `collectScheduledMidiPlaybackEventsForLoopedBlock`；不得把跨循环 tick 窗口当作一个连续区间处理，新增 tempo 或跨边界规则必须补确定性测试。
- 实现按轨道分发 MIDI 输出时：
  - 触发场景：`MidiDispatch` 和 `AudioEngineMidiBridge` 已经能把 block 内事件发送给一个抽象接收器后，需要为每条乐器轨未来接入不同插件、采样器或外部 MIDI 目标预留稳定分发入口。
  - 根本原因：如果所有 MIDI 事件长期只发往一个接收器，轨道到输出目标的关系会被隐藏；如果调用方先按轨道分组再多次 dispatch，又会破坏原始事件顺序并重复实现缺失路由和失败语义。
  - 采用的解决方式：新增实现 `MidiEventReceiver` 的 `MidiTrackRouter`，在播放前验证并重建 `trackId -> receiver` 绑定，事件接收阶段只做只读查找和转发；用 CTest 覆盖按轨道分发、缺失路由、下游失败、非法绑定不替换旧路由和空绑定。
  - 后续规则：未来接入插件乐器、采样器或外部 MIDI 目标时，应通过按轨道 ID 绑定的路由层分发，不要绕过 `MidiDispatch` 的消息转换和失败统计；绑定更新必须在播放前完成，非法绑定不能替换仍可用的旧路由。
- 构建项目级 MIDI 输出图时：
  - 触发场景：已有 `MidiTrackRouter` 后，需要把轨道绑定和工程模型连接起来，防止后续调用方把 MIDI 输出接到不存在轨道、音频轨或文件夹轨。
  - 根本原因：轨道 ID 到接收器的纯路由层不知道 `Project` 中的轨道类型；如果项目级校验分散在调用方，后续插件、采样器和外部设备接入时容易出现重复校验或漏校验。
  - 采用的解决方式：新增 `ProjectMidiOutputGraph`，在 `rebuild` 时验证绑定轨道必须存在且为 `TrackType::Instrument`，再复用 `MidiTrackRouter` 保存路由，并通过 `AudioEngineMidiBridge` 分发已渲染的 scheduled MIDI 事件；用 CTest 覆盖合法乐器轨分发、非法绑定不替换旧路由、未绑定渲染轨道、下游失败和空绑定。
  - 后续规则：项目级 MIDI 输出必须先经过工程轨道类型校验，再进入按轨道路由；音频轨和文件夹轨不能绑定 MIDI 输出接收器；`ProjectMidiOutputGraph` 不重新判断静音、独奏、禁用或隐藏状态，这些播放语义继续由 `MidiPlayback` 决定。
- 管理 MIDI 输出活动音符时：
  - 触发场景：项目级 MIDI 输出图已经能分发 rendered scheduled MIDI 事件后，需要在未来接真实设备、插件乐器或采样器前记录哪些 Note On 已经成功送达，并能在停止播放或换路由前释放。
  - 根本原因：一次性分发函数没有活动音符状态；如果 Note Off 之前重建路由，音符释放可能发到错误目标或没有目标；如果接收器失败后仍更新状态，会把未真正送达的事件误认为正在响或已经释放。
  - 采用的解决方式：新增 `MidiOutputSession`，内部拥有 `ProjectMidiOutputGraph`，只根据 `deliveredEventCount` 范围内的事件维护活动音符；有活动音符时拒绝 `rebuild`；`releaseAllActiveNotes` 生成 Note Off，并只清除成功送达的释放事件。自审时补充了同一轨道、通道和音高重复 Note On 的测试，确认紧急释放一次 Note Off 后清除整个活动键。
  - 后续规则：真实 MIDI 设备、插件乐器或采样器接入时，不得在活动音符仍存在时切换输出路由；停止播放、切换目标或销毁输出前必须先通过会话释放活动音符；释放失败时必须保留未送达音符状态并继续阻止路由重建。
- 协调项目播放 block 时：
  - 触发场景：`AudioEngine`、`ProjectPlaybackGraph` 和 `MidiOutputSession` 都已存在后，需要一个上层核心入口把音频渲染、MIDI 事件收集和 MIDI 输出分发按同一 block 生命周期串起来。
  - 根本原因：如果 UI、设备层或插件层各自手动组合这些模块，容易把 MIDI 输出失败误当成音频渲染失败，或者在音频源失败时仍发送已经收集但无效的 MIDI 事件。
  - 采用的解决方式：新增 `ProjectPlaybackSession`，内部拥有音频引擎、项目音频图和 MIDI 输出会话；`renderNextBlock` 先渲染音频并收集 scheduled MIDI，只有渲染成功才分发 MIDI；返回值分开报告 `renderSucceeded` 和 `midiDispatch`。自审时补充活动 MIDI 音符期间重新 `prepare` 的测试，并改为拒绝该操作且保留旧配置。
  - 后续规则：播放协调层必须分开处理音频渲染结果和 MIDI 输出结果；MIDI 输出失败不能回滚 `Transport` 或清掉已渲染音频；音频渲染失败不能发送该 block 的 MIDI；活动 MIDI 音符释放前不得重新准备播放会话或切换会影响输出一致性的配置。
- 接入循环 MIDI 渲染路径时：
  - 触发场景：循环 `PlaybackClock` 已经能输出跨循环 scheduled MIDI 事件后，需要让 `AudioEngine` 和 `ProjectPlaybackSession` 使用这些事件完成一次 block 的项目级分发。
  - 根本原因：如果直接修改普通 `renderNextBlockWithMidi`，普通播放会隐式变成循环语义；如果把循环逻辑只放在会话层，又会重复音频引擎已有的清结果、渲染失败回滚和 transport 推进规则。
  - 采用的解决方式：新增显式 `renderNextBlockWithLoopedMidi` 和 `renderNextLoopedBlock`，普通播放入口保持不变；循环入口只替换 MIDI 收集函数，继续沿用音频源失败不发送 MIDI、MIDI 分发失败不回滚音频和 transport 的规则；用 CTest 覆盖跨循环 MIDI 分发、block-wide sample offset、非法 loop 不推进和不分发。
  - 后续规则：未来 UI 循环区域、循环编曲器或设备层接入循环播放时，应调用显式 loop-aware 入口；不得把循环范围作为隐藏全局状态塞进普通播放函数。当前能力只保证 MIDI 调度和分发循环化，不得宣称音频素材、插件尾音、chase 或延迟补偿已经完成。
- 释放循环边界长音符时：
  - 触发场景：循环 block 在回绕前窗口内触发一个长 MIDI 音符，但该音符真实 Note Off 落在循环右边界之后，`MidiOutputSession` 会收到 Note On 却收不到对应 Note Off。
  - 根本原因：普通 MIDI 调度使用 `[startTick, endTick)` 半开窗口，循环右边界的真实 Note Off 被排除；回绕后的窗口从循环起点重新开始，也不会自然收集这个超出边界的释放事件。
  - 采用的解决方式：在 `PlaybackClock` 的循环 MIDI 收集路径中，只针对结束于 `loopRange.endTick` 的子窗口，为该子窗口内真实收集到的 Note On 合成一个 `absoluteTick == loopRange.endTick` 的 Note Off，并把 sample offset 夹到回绕前最后一个 sample；用 CTest 覆盖调度事件和 `ProjectPlaybackSession` 活动音符清零。
  - 后续规则：循环边界释放不得改动普通播放半开窗口语义；不得给 block 开始前已经激活但未被当前窗口收集到的音符生成孤立 Note Off，除非后续明确实现 MIDI chase 并补对应测试。
- 实现 MIDI chase 播放时：
  - 触发场景：播放从一个 MIDI 长音符中间开始，或循环回绕到一个已经持续中的音符内部时，旧调度只会在真实 Note On 落入窗口时发声，导致从中间开始播放时听不到该音符。
  - 根本原因：`collectMidiPlaybackEvents` 是纯半开窗口收集器，只返回 `[startTick, endTick)` 内真实存在的 Note On/Off；它适合做基础确定性语义，但不等同于实际播放所需的 chase 行为。
  - 采用的解决方式：新增 `collectMidiPlaybackEventsWithChase`，只在窗口起点为 `noteOnTick < startTick && noteOffTick > startTick` 的音符补发 Note On；`PlaybackClock` 新增 `MidiChaseMode`，默认关闭，只有显式启用时才 chase；`ProjectPlaybackSession` 用一次性 chase 标志处理起播、MIDI 输出重建、释放或显式请求后的下一帧。自审发现无条件 chase 会导致连续 block 重复 Note On 后，补充连续 block 回归测试并修正。
  - 后续规则：新增播放输出入口时应明确选择纯半开收集还是一次性 chase-aware 收集；不得让连续播放的每个 block 自动重复 chase 同一长音符。当前 chase 只覆盖 Note On，不覆盖踏板、控制器、Pitch Bend、Aftertouch、插件状态或音频素材，未实现部分不得对外宣称已经支持。
- 停止或跳转播放前释放活动 MIDI 音符时：
  - 触发场景：`ProjectPlaybackSession` 中已有活动 Note On 时，如果直接停止或跳转播放，输出设备、插件乐器或采样器可能保留悬挂音符。
  - 根本原因：`Transport` 只管理播放头和播放状态，不知道 MIDI 输出端已经成功送达哪些 Note On；`MidiOutputSession` 才持有活动音符状态。
  - 采用的解决方式：在 `ProjectPlaybackSession` 增加 `stopPlayback` 和 `seekPlaybackToSample`，先调用 `releaseActiveMidiNotes`，只有释放成功才改变 `Transport`；跳转成功后请求下一帧 chase。CTest 覆盖释放成功后停止、释放失败不停止、释放后跳转和非法跳转不改播放头。
  - 后续规则：所有会改变播放头连续性的上层操作必须经过会话层或等价释放边界；释放失败时不得移动播放头、停止播放或切换输出路由。
- 安全切换 MIDI 输出路由时：
  - 触发场景：播放会话中已有活动 Note On 时，用户或后续设备层需要把乐器轨 MIDI 输出从旧接收器切换到新设备、插件乐器或采样器。
  - 根本原因：低层 `MidiOutputSession::rebuild` 能拒绝活动音符期间直接换路由，但调用方仍可能忘记先通过旧路由释放音符，或在释放失败、绑定无效、需要 chase 时各自实现出不一致的错误处理。
  - 采用的解决方式：在 `ProjectPlaybackSession` 增加 `rebuildMidiOutputSafely`，先调用 `releaseActiveMidiNotes`，释放失败则不换路由；释放成功后再调用现有 `rebuildMidiOutput`，新绑定无效时保留旧路由，成功或释放成功后由会话层请求下一帧 chase。CTest 覆盖成功切换、释放失败保留旧路由、绑定无效保留旧路由和未 prepare 拒绝调用。
  - 后续规则：播放中或播放边界附近切换 MIDI 设备、插件乐器、采样器或等价输出目标时，必须走会话层安全重建入口或等价释放边界；释放失败不得替换路由，无效绑定不得破坏旧路由，成功释放后需要保留下一帧 chase 语义。
- 建立播放控制命令层时：
  - 触发场景：停止、跳转和 MIDI 输出重建已经各自有安全会话入口后，需要给 UI、快捷键、设备层和 AI 工具提供统一调用边界。
  - 根本原因：现有 `Command` / `CommandStack` 负责工程数据修改和撤销重做；播放头、活动 MIDI 音符和输出路由属于运行态，把它们放进工程撤销栈会混淆“编辑历史”和“播放控制历史”。
  - 采用的解决方式：新增独立 `PlaybackControlCommand` 层，提供 `StopPlaybackCommand`、`SeekPlaybackCommand` 和 `RebuildMidiOutputCommand`，只包装 `ProjectPlaybackSession` 的安全入口，不修改工程文件内容，不参与撤销重做。CTest 覆盖停止、跳转、非法跳转不释放、路由安全切换和未 prepare 拒绝。
  - 后续规则：运行态播放控制命令必须与工程编辑命令分离；需要撤销重做的是工程数据变化，需要安全释放和故障隔离的是播放会话状态，两者不得共用同一个 `CommandStack`。
- 完善播放控制错误分类时：
  - 触发场景：阶段 B 开始面向 UI、快捷键、设备层和 AI 工具暴露播放控制结果，旧 `PlaybackControlResult` 只有 `success` 和自由文本 `message`，底层失败域也主要通过 `bool` 表达。
  - 根本原因：自由文本适合给人阅读，但不适合作为程序判断依据；如果 UI 或 AI 工具解析字符串来区分“未 prepare”“非法 seek”“MIDI 释放失败”“输出绑定无效”，后续本地化或文案调整会破坏控制逻辑。
  - 采用的解决方式：新增 `PlaybackControlFailureReason`、`ProjectPlaybackControlFailureReason` 和 `ProjectPlaybackMidiOutputRebuildFailureReason` 枚举，把命令层和会话层失败原因分开记录；CTest 覆盖未 prepare、非法 seek、MIDI 释放失败和 MIDI 输出重建绑定拒绝。
  - 后续规则：播放控制、设备层、UI、快捷键和 AI 工具必须读取稳定 reason enum 做逻辑分支；`message` 只用于显示或日志。新增播放控制失败路径时，必须先补枚举值或复用准确枚举值，并添加失败原因测试。
- 接入桌面播放/停止入口时：
  - 触发场景：阶段 C 首屏已有工程、轨道、MIDI 片段和默认音符编辑入口后，需要把“播放/停止”按钮接入桌面壳，但真实音频设备回调尚未实现。
  - 根本原因：如果 UI 直接持有或修改 `Transport`，会把播放运行态、核心安全停止命令、dirty 状态和反馈文案混在界面层；如果直接宣称完整播放，又会掩盖没有声卡回调、插件和硬件输出的事实。
  - 采用的解决方式：新增 `AppPlaybackActions` 和 `AppPlaybackController`，由应用层保存运行态 `ProjectPlaybackSession` 与 `Transport`；开始播放只准备空项目播放会话并切换播放状态，停止播放复用 `StopPlaybackCommand`；CTest 覆盖开始、停止、未开始时停止、状态描述和不标脏工程。
  - 后续规则：桌面 UI、快捷键或 AI 工具触发播放控制时，应先经过 `AppPlaybackActions` 或等价应用层入口；播放运行态不得写入 `Project` 或标脏工程；没有真实音频设备回调前，不得把当前播放按钮描述为可听音频输出、插件播放或硬件 MIDI 输出。
- 用桌面 UI Timer 推进播放位置时：
  - 触发场景：首屏已有播放/停止按钮后，用户需要看到播放位置变化，但当前阶段还没有真实音频设备回调。
  - 根本原因：如果为了显示播放位置而让 UI 直接修改 `Transport`，会绕过已经测试的 `ProjectPlaybackSession::renderNextBlock`；如果把 UI Timer 伪装成真实音频回调，又会让后续声卡接入边界变模糊。
  - 采用的解决方式：新增 `advanceAppPlaybackForUiTick`，通过应用层持有的静音 scratch buffer 调用项目播放会话推进一个 UI block；停止状态返回稳定 no-op；JUCE 首屏只用 `Timer` 调用该入口并刷新 `AppPlaybackStatus`。CTest 覆盖播放中推进、停止时不准备运行态、不标脏工程和状态摘要秒数。
  - 后续规则：在真实音频设备接入前，UI Timer 只用于可见播放头推进和界面验证；不得宣称它等同于低延迟音频回调、可听输出或硬件 MIDI 输出。真实声卡接入时必须改由设备 block 回调驱动播放会话，并保留应用层状态描述和失败分类测试。
- 接入桌面“回到开头”入口时：
  - 触发场景：首屏已有播放、停止和可见播放位置后，需要让用户把播放头安全回到 sample 0。
  - 根本原因：如果 UI 直接调用 `Transport::seekToSample(0)`，会绕过活动 MIDI 音符释放、seek 失败原因和会话层 chase 规则；如果把回到开头做成工程编辑命令，又会把运行态控制错误放进工程撤销栈。
  - 采用的解决方式：新增 `rewindAppPlaybackToStart`，应用层先确保播放会话已准备，再复用核心 `SeekPlaybackCommand` 跳转到 `defaultAppPlaybackStartSample`；JUCE 首屏新增“回到开头”按钮。CTest 覆盖播放中回到开头且继续播放、停止后回到开头、初始运行态回到开头只准备不播放，并验证不标脏工程。
  - 后续规则：所有桌面 seek、回到开头、快捷键跳转或 AI 播放头控制都应复用 `SeekPlaybackCommand` 或等价安全入口；不得在 UI 层直接改 `Transport` 位置，也不得把播放头移动写入 `CommandStack`。
- 接入桌面播放/停止切换时：
  - 触发场景：首屏已有独立播放和停止按钮后，需要给空格快捷键、菜单和后续 AI 工具提供一个统一的“播放/停止切换”入口。
  - 根本原因：如果每个调用方都自己判断 `isPlaying()` 再分别调用开始或停止，Timer 启停、dirty 状态、反馈文案和停止安全边界会分散，后续快捷键和菜单容易与按钮行为不一致。
  - 采用的解决方式：新增 `toggleAppPlayback`，应用层根据当前播放状态复用 `startAppPlayback` 或 `stopAppPlayback`；JUCE 首屏用空格键调用统一切换入口，并按结果启动或停止 UI Timer。CTest 覆盖停止态切换为播放、播放态切换为停止、播放位置推进后切换停止仍保留当前位置且不标脏工程。
  - 后续规则：菜单、快捷键、控制面板和 AI 工具需要“播放/停止切换”时，应调用 `toggleAppPlayback` 或等价应用层入口；不要在 UI 各处复制 `isPlaying()` 分支，也不要绕过 `StopPlaybackCommand` 的安全停止路径。
- 建立真实 MIDI 输出设备抽象层时：
  - 触发场景：已有 `MidiDispatch`、轨道路由和项目输出图后，需要为真实 MIDI 设备接入准备边界，同时不能让核心调度直接依赖 JUCE、Windows MIDI API 或具体设备句柄。
  - 根本原因：如果在 `MidiDispatch` 或 `AudioEngine` 内直接打开和发送真实设备，会把平台 I/O、设备生命周期、MIDI 消息转换、调度失败统计和播放状态混在一起，后续测试、断开重连和插件/采样器输出都会变复杂。
  - 采用的解决方式：新增 `MidiOutputDevicePort` 平台端口接口和实现 `MidiEventReceiver` 的 `MidiOutputDevice` 适配器；`MidiDispatch` 继续负责事件到 `MidiOutputMessage` 的转换和失败索引统计，设备层只负责打开、关闭、发送和记录 `MidiOutputDeviceFailureReason`。CTest 覆盖打开发送、未打开拒绝、打开失败、关闭后拒绝、重开后发送和发送失败停止 dispatch。
  - 后续规则：核心库中的 MIDI 设备层只能依赖端口接口，不直接包含 JUCE 或 Windows 设备 API；真实设备适配层必须把平台失败映射到稳定 reason enum，并继续通过 `MidiDispatch` 进入项目路由和活动音符管理。
- 接入 JUCE MIDI 输出端口时：
  - 触发场景：核心 MIDI 输出设备抽象完成后，需要在 Windows 上通过 JUCE 枚举真实 MIDI 输出端口，并按稳定设备 id 打开、关闭和发送消息。
  - 根本原因：如果让 `trackloom_core` 直接包含 JUCE 头文件或链接 JUCE 模块，核心库会失去可测试的平台无关边界；如果测试依赖外接 MIDI 设备，普通开发环境会因为没有硬件而无法验证。
  - 采用的解决方式：新增独立 `trackloom_juce` 平台适配库和 `JuceMidiOutputPort`，CMake 通过 `TRACKLOOM_JUCE_ROOT` 或 `JUCE_ROOT` 引入本地 JUCE 8；核心库只继续依赖 `MidiOutputDevicePort`。CTest 覆盖 JUCE 设备信息映射、核心 MIDI 三字节消息转换、无硬件假设的枚举、无效设备 id 打开失败和端口工厂。
  - 后续规则：JUCE、Windows MIDI API、插件 SDK 等平台依赖必须放在 `src/platform/` 或等价适配层，不得反向进入 `src/core`；自动测试优先覆盖不依赖硬件的确定性边界，真实外设发送、热插拔和断开重连需要单独冒烟测试；JUCE `sendMessageNow` 不返回发送失败布尔值，不能把调用成功误写成设备仍然在线。
- 管理真实 MIDI 输出设备生命周期时：
  - 触发场景：JUCE MIDI 输出端口已经能打开单个设备后，需要把多条乐器轨的设备选择接入 `ProjectPlaybackSession`，并在切换设备前释放旧设备上的活动音符。
  - 根本原因：播放会话的 MIDI 路由只保存 `MidiEventReceiver*` 非拥有指针；如果设备管理器没有稳定拥有端口和 `MidiOutputDevice`，或在失败时替换旧资源，就会产生悬空指针、悬挂音符或旧路由丢失。
  - 采用的解决方式：新增 `JuceMidiOutputDeviceManager`，先验证轨道绑定和设备 id，再通过可替换端口工厂打开设备，最后调用 `ProjectPlaybackSession::rebuildMidiOutputSafely`；只有安全重建成功才替换并关闭旧设备，失败时关闭临时新设备并保留旧设备。CTest 覆盖接入播放会话、打开失败保留旧设备、切换前发送 Note Off、清空输出关闭设备和非法轨道不触发端口打开。
  - 后续规则：设备管理器必须比使用它的播放会话路由活得更久，且不允许随意复制或移动；所有真实设备切换都必须走安全重建边界；非法轨道或空设备 id 不得触发外部 I/O；真实硬件测试要单独标记，不能让普通 CTest 依赖外接设备。
- 建立外接 MIDI 硬件冒烟测试时：
  - 触发场景：JUCE MIDI 输出设备管理器完成后，需要验证真实设备发送路径，同时不能让没有外接硬件的普通开发环境失败。
  - 根本原因：真实 MIDI 发送会产生外部声音或设备状态变化；如果自动 CTest 默认发送到系统设备，可能误触发用户硬件，也会让 CI 或无设备机器不稳定。
  - 采用的解决方式：新增 `trackloom_midi_hardware_smoke_tests`，CTest 设置 `SKIP_RETURN_CODE 77` 和 `manual;hardware` 标签；默认跳过并打印可见设备列表，只有同时设置 `TRACKLOOM_MIDI_HARDWARE_SMOKE=1` 与 `TRACKLOOM_MIDI_OUTPUT_ID` 才通过 `JuceMidiOutputDeviceManager` 发送短 Note On，并在结束时清空路由释放 Note Off。
  - 后续规则：普通 CTest 不得依赖真实 MIDI 硬件；硬件冒烟测试必须显式指定设备 id、打印可见设备、限制发声时长、结束前释放活动音符，并把未启用或未指定设备视为跳过而不是失败。
- 识别 MIDI 输出设备列表变化时：
  - 触发场景：硬件冒烟测试入口完成后，需要为 UI 设备列表刷新、热插拔提示和设备断开处理准备可测试的底层规则。
  - 根本原因：如果每个调用方各自比较设备列表，可能用显示名当身份、顺序不稳定，或者在设备重命名时误判为移除再新增。
  - 采用的解决方式：新增 `diffMidiOutputDeviceLists` 纯函数，按稳定设备 id 比较前后两次快照；新增和保留设备按当前列表顺序输出，移除设备按旧列表顺序输出，保留设备使用当前快照中的显示名。CTest 覆盖重命名、新增、移除和顺序规则。
  - 后续规则：UI、设备刷新和热插拔处理必须复用同一套快照差异规则；设备身份以 id 为准，显示名只用于展示；后台轮询或系统通知层不得直接打开、关闭或切换输出路由，真实路由变化仍必须走安全释放边界。
- 建立 MIDI 输出设备列表刷新入口时：
  - 触发场景：已有设备快照差异函数后，需要给后续 UI 或设置层一个稳定入口来刷新当前设备列表，而不是让调用方各自保存旧快照。
  - 根本原因：如果 UI、设备设置和热插拔处理分别维护设备缓存，容易出现枚举次数不可控、缓存和差异不一致、或查找设备时隐式重新枚举系统设备。
  - 采用的解决方式：新增 `JuceMidiOutputDeviceList`，通过可注入枚举函数刷新设备快照；`refresh` 每次只枚举一次并返回差异，`devices` 暴露当前缓存，`findById` 只查询缓存不触发系统枚举。CTest 覆盖首次刷新、后续刷新、缓存替换和查找不重新枚举。
  - 后续规则：UI 设备列表和后续热插拔刷新入口应先通过该状态类获取差异，再决定提示或路由处理；缓存刷新不得自动打开设备或切换轨道路由，设备移除后的释放和绑定策略必须在更高层显式处理并测试。
- 规划 MIDI 输出设备移除策略时：
  - 触发场景：设备列表能刷新并返回差异后，需要决定轨道绑定指向已消失设备时如何处理，同时不能在纯刷新层直接操作播放会话。
  - 根本原因：如果设备消失时立即删除用户绑定，用户重新插回设备后难以恢复选择；如果继续把消失设备当作可路由目标，后续重建会尝试打开不可用设备，或者把 UI 状态和运行态路由混在一起。
  - 采用的解决方式：新增 `planMidiOutputDeviceBindingRefresh` 纯函数，把绑定分成 `availableBindings` 和 `unavailableBindings`；可见设备使用最新设备信息，不可见设备保留原绑定信息，并在存在不可用绑定时标记 `requiresSafeRebuild`。CTest 覆盖设备消失、绑定顺序、显示名更新、不可用绑定保留和全部设备可见时不要求重建。
  - 后续规则：设备消失时，运行态路由只能使用 `availableBindings`；用户选择和 UI 提示应保留 `unavailableBindings` 中的原设备信息；安全释放、路由切换和失败回滚必须由更高层通过 `rebuildProjectMidiOutputSafely` 或等价安全边界完成。
- 连接设备刷新结果到安全重建时：
  - 触发场景：设备移除策略已经能标记哪些绑定不可用后，需要把该计划真正应用到 `JuceMidiOutputDeviceManager`，让运行态路由排除已消失设备。
  - 根本原因：如果设备刷新层直接关闭端口或删除路由，活动音符可能无法释放，且 UI 也会丢失原设备绑定；如果所有设备仍可见时也重建路由，又会因为显示名变化等无害刷新打断播放输出。
  - 采用的解决方式：新增 `refreshProjectMidiOutputForVisibleDevicesSafely`，先生成绑定刷新计划；只有存在不可用绑定时才调用安全重建，并继续复用活动音符释放和失败回滚语义；全部设备仍可见时只返回最新计划，不重新打开端口。
  - 后续规则：热插拔、设备列表刷新和 UI 设备设置接入时，不得在刷新回调里直接关闭旧设备或清空路由；必须先生成绑定计划，再通过安全重建应用可用绑定。释放失败时保留旧设备、旧路由和活动音符状态，方便下一次释放或用户处理。
- 建立无 UI 的 MIDI 输出设备选择控制层时：
  - 触发场景：设备列表、设备移除计划和安全重建入口都已存在后，需要给后续 UI、快捷键或 AI 工具一个稳定入口来保存轨道设备选择并应用刷新结果。
  - 根本原因：用户选择和运行态路由不是同一个状态。设备消失时如果删除用户选择，用户无法在设备重新插回后恢复；如果把消失设备继续留在运行态路由里，又会错误尝试打开不可用设备。
  - 采用的解决方式：新增 `JuceMidiOutputRoutingController`，内部保存完整用户选择；刷新设备时只更新仍可见设备的显示信息，保留不可见设备的原选择；运行态路由通过安全重建只使用当前可见绑定。CTest 覆盖设备消失保留选择但收缩路由、显示名变化不重开端口、清除选择后关闭运行态设备。
  - 后续规则：后续 UI 或设置持久化层应把“用户选择”“当前可见设备”“当前打开路由”分开显示和保存；清除选择才代表用户放弃绑定，设备暂时不可见只能提示和暂停运行态路由，不能自动删除用户选择。
- 恢复重新出现的 MIDI 输出设备时：
  - 触发场景：无 UI 设备选择控制层已经能在设备消失时保留用户选择并收缩运行态路由后，设备重新出现时，用户选择重新变成全部可见，但运行态路由仍可能停留在收缩后的设备集合。
  - 根本原因：只用“是否存在不可用绑定”判断是否重建不够；设备重新出现后不可用绑定数量为零，但当前打开路由可能仍缺少刚恢复的设备。
  - 采用的解决方式：控制层刷新设备后同时比较“当前可用选择”和“当前打开设备路由”；只要二者不一致，就通过安全重建恢复运行态路由。CTest 覆盖 A+B 在线、B 消失、B 重新出现的三段快照，并确认 B 重新出现后恢复两条运行态路由。
  - 后续规则：热插拔恢复逻辑必须同时看设备列表差异、用户选择和当前打开路由；不得把“所有选择设备当前可见”当成“运行态路由已经恢复”。恢复路由仍必须走安全释放边界，不能直接补开设备绕过会话状态。
- 判断 MIDI 输出运行态路由是否匹配时：
  - 触发场景：无 UI 设备选择控制层已经能恢复重新出现的设备后，测试同一个设备从 Lead 轨改绑到 Bass 轨，设备 id 不变但轨道绑定已经改变。
  - 根本原因：只比较当前打开设备 id 会把“同一设备仍打开”误判为“轨道路由已经正确”。播放会话内部保存的是 `trackId -> receiver`，同一设备改绑到另一条轨道必须重建该映射。
  - 采用的解决方式：控制层记录上一次成功应用到播放会话的绑定，并用 `trackId + deviceId` 与当前可用选择比较；显示名变化不触发重建，同设备换轨道会触发安全重建。CTest 覆盖同设备从 Lead 改绑到 Bass 后，Bass 轨 MIDI 能通过该设备发送。
  - 后续规则：任何判断运行态 MIDI 输出是否已经同步的逻辑，都必须比较轨道 ID 和设备 ID，不能只看设备 ID、设备名称或打开端口数量；成功安全重建后才允许更新“已应用绑定”状态。
- 按设备 ID 设置 MIDI 输出选择时：
  - 触发场景：无 UI 设备选择控制层准备给后续界面使用后，需要让 UI 只提交用户选择的设备 id，而不是自己构造 `MidiOutputDeviceInfo`。
  - 根本原因：设备显示名来自最新枚举缓存，UI 手工拼接设备结构容易保存过期显示名；如果选择动作隐式重新枚举或直接打开设备，又会让普通选择和运行态路由切换耦合。
  - 采用的解决方式：新增 `JuceMidiOutputRoutingController::setTrackOutputDeviceById`，只从 `refreshDevices` 建立的缓存查找设备；缓存中没有该 id 时返回失败并保留原选择，不重新枚举、不打开端口。CTest 覆盖按缓存 id 选择、使用当前显示名、缺失设备 id 不覆盖旧选择和选择过程不重新枚举。
  - 后续规则：UI、快捷键或 AI 工具按设备 id 设置轨道输出时，应先刷新或读取当前设备缓存，再调用控制层选择接口；选择失败不得清空旧绑定，真正的端口打开、关闭和活动音符释放仍必须通过安全重建入口完成。
- 暴露 MIDI 输出路由状态给 UI 时：
  - 触发场景：无 UI 设备选择控制层已经保存用户选择、可见设备缓存和已应用路由后，后续 UI 需要展示“已选择但设备离线”“已选择并已应用”“可见但未打开”等状态。
  - 根本原因：如果 UI 通过多个零散接口自行推断状态，容易把用户选择、可见设备、已应用轨道路由和当前打开端口混为一谈；如果 UI 反查播放会话内部状态，又会破坏设备控制层边界。
  - 采用的解决方式：新增 `MidiOutputRoutingState` 和 `JuceMidiOutputRoutingController::routingState`，一次性返回 `selectedBindings`、`visibleDevices`、`appliedBindings` 和 `openDevices` 的值类型快照；该接口不枚举设备、不打开端口、不重建路由。CTest 覆盖选择后未应用、成功应用后、设备消失后收缩路由三种状态。
  - 后续规则：UI、诊断面板或 AI 工具展示 MIDI 输出设备状态时，应读取控制层快照；不得把打开端口数量等同于用户选择，也不得绕过控制层读取或修改播放会话内部路由。
- 展示 MIDI 输出路由状态分类时：
  - 触发场景：控制层已经能提供只读路由快照后，UI 仍需要把多组列表转换成“已应用”“待应用”“设备离线”“已应用但端口缺失”“旧路由待移除”等用户可理解状态，并判断“应用更改”按钮是否应该启用。
  - 根本原因：如果每个 UI 面板自行比较选择、可见设备、已应用绑定和打开端口，规则会分散且容易不一致；尤其是“设备可见但未应用”“设备离线”和“用户已清除选择但旧运行态路由尚未安全重建移除”不能只靠打开端口数量判断。
  - 采用的解决方式：新增 `MidiOutputRouteStatus`、`MidiOutputRouteStatusDescription` 和纯函数 `describeMidiOutputRoutingState`，先按用户选择顺序输出每条选择的展示状态，再追加未被当前选择覆盖的已应用旧路由；再新增 `midiOutputRouteStatusRequiresApply` 和 `midiOutputRoutingStateRequiresApply`，把“是否需要应用更改”的规则收敛到同一处。CTest 覆盖已应用、待应用、设备离线、已应用但打开端口缺失、旧路由待移除，以及整体路由状态是否需要应用。
  - 后续规则：UI、诊断面板和 AI 工具显示 MIDI 路由健康状态时，应复用该纯函数；启用或禁用“应用更改”按钮时，应复用统一的 apply 判断。新增状态类型时必须补覆盖所有分类分支和 apply 语义的测试，不允许把同一套比较逻辑复制到多个界面。
- 应用 MIDI 输出路由选择时：
  - 触发场景：状态分类已经能告诉 UI 是否需要应用更改后，还需要一个无 UI 的应用入口，避免界面层直接调用底层重建并在无变化时反复重开端口。
  - 根本原因：`rebuildSelectedProjectMidiOutputSafely` 是底层强制重建入口，适合明确需要切换路由的调用；如果 UI 的“应用更改”按钮直接调用它，已应用状态也会发生设备关闭和重开，增加真实硬件抖动和悬挂音符风险。
  - 采用的解决方式：新增 `JuceMidiOutputRoutingController::applySelectedProjectMidiOutputIfNeeded`，先读取路由快照判断是否需要应用；无变化时成功返回但不重建，有待应用或待移除旧路由时才通过安全重建更新运行态。CTest 覆盖已应用状态无操作和清除选择后安全关闭旧路由。
  - 后续规则：UI、快捷键和 AI 工具执行“应用当前 MIDI 输出选择”时，应优先使用按需应用入口；只有明确需要强制重建的底层场景才直接调用强制重建函数。
- 刷新 MIDI 输出设备列表时：
  - 触发场景：用户在设备在线时选择了输出设备，但尚未应用到运行态；随后设备离线，刷新控制层时传入的播放会话可能还没有准备。
  - 根本原因：设备刷新计划只知道“选择里的设备离线”，不知道这条选择是否已经应用到运行态；如果把所有离线选择都当作必须安全重建，会让未应用的离线选择错误依赖播放会话准备状态。
  - 采用的解决方式：控制层刷新时先比较“当前可路由选择”和“已应用绑定”。只有运行态与可路由选择不一致时才安全重建；未应用且离线的选择只保留给 UI 提示和未来重连。CTest 覆盖未应用离线选择在未准备播放会话下刷新成功且不打开端口。
  - 后续规则：设备热插拔处理必须区分用户选择、可见设备和已应用运行态路由；离线提示不等于需要触碰播放会话，只有已应用路由需要释放或收缩时才走安全重建。
- 汇总 MIDI 输出刷新结果时：
  - 触发场景：控制层刷新结果已经同时包含设备差异、离线选择、安全重建尝试和失败原因，后续 UI/诊断面板需要快速判断是否显示提示。
  - 根本原因：如果每个界面都自行拆 `deviceDiff` 和 `outputRefresh`，同一个刷新结果可能被解释成不同提示；尤其是“设备列表变化”“离线选择”和“安全重建失败”应当有统一的注意事项判断。
  - 采用的解决方式：新增 `MidiOutputRoutingRefreshSummary` 和 `summarizeMidiOutputRoutingRefreshResult`，把设备新增/移除、离线选择、安全重建尝试和安全重建失败汇总为只读布尔摘要。CTest 覆盖无提示刷新、离线选择提示和安全重建失败提示。
  - 后续规则：UI、诊断面板和 AI 工具展示 MIDI 设备刷新结果时，应优先读取摘要 helper；详细列表仍从原始 refresh result 读取，不在摘要里复制完整设备数据。
- 汇总 MIDI 输出应用结果时：
  - 触发场景：控制层已有按需应用入口后，UI 仍需要区分“无变化但成功”“确实应用了运行态变化”“离线选择需要提示”和“安全重建失败”。
  - 根本原因：如果界面只看 `success`，会把成功 no-op 和成功重建混在一起；如果界面直接拆 `bindingPlan`、`safeRebuildAttempted` 和失败原因，又会复制与诊断面板相同的判断逻辑。
  - 采用的解决方式：新增 `MidiOutputRoutingApplySummary` 和 `summarizeMidiOutputRoutingApplyResult`，把应用结果折叠成成功、离线选择、安全重建尝试、安全重建失败、是否应用运行态变化和是否需要用户注意。CTest 覆盖无变化应用、离线选择、成功重建和失败重建。
  - 后续规则：UI、诊断面板和 AI 工具展示“应用 MIDI 输出选择”的结果时，应优先读取 apply summary；是否显示详细设备和失败原因仍从原始 apply result 读取。
- 接入 JUCE GUI 桌面壳时：
  - 触发场景：新增 `trackloom_app` 并链接 `juce_gui_basics` 后，原本只声明 `LANGUAGES CXX` 的顶层 CMake 在重新生成时失败，报缺少 `CMAKE_C_COMPILE_OBJECT`。
  - 根本原因：JUCE GUI 依赖中包含 C 源文件，例如 `juce_graphics_Sheenbidi.c`；顶层工程只启用 C++ 语言时，CMake 没有 C 编译规则。
  - 采用的解决方式：将顶层 `project(TrackLoom ...)` 调整为 `LANGUAGES C CXX`，并继续让核心业务源码保持 C++20；随后 `trackloom_app_support_tests`、`trackloom_app` 构建和隐藏启动验证通过。
  - 后续规则：凡是新增或调整 JUCE GUI、图形、字体、Web、插件宿主等可能包含 C/C++ 混合源的目标时，必须保留顶层 C 语言支持；不得把该问题误判为源码或 MSVC 安装损坏。
- 管理桌面壳当前工程状态时：
  - 触发场景：阶段 C 开始接入工程新建、保存和打开能力，需要让桌面壳知道当前工程、当前文件路径以及是否有未保存修改。
  - 根本原因：`Project` 和 `ProjectFile` 只负责工程数据和文件读写，不应承担应用窗口当前路径、dirty 状态或打开失败回滚等运行态职责；如果 UI 直接拼接这些状态，后续菜单、快捷键和 AI 工具会重复实现且容易不一致。
  - 采用的解决方式：新增 `AppProjectSession` 应用层会话，复用 `saveProjectToFileAtomically` 和 `loadProjectFromFile`，在保存成功后更新路径并清除 dirty，在打开失败时保留当前工程、路径和 dirty 状态。CTest 覆盖新建、编辑标脏、另存为、打开、打开失败和无路径保存失败。
  - 后续规则：桌面 UI、快捷键和 AI 工具需要操作当前工程文件时，应优先经过 `AppProjectSession` 或等价应用层边界；不要把当前文件路径、dirty 标志或 UI 打开状态写入核心 `Project` 格式。
- 展示桌面壳工程状态时：
  - 触发场景：`AppProjectSession` 已能管理当前工程后，需要让 JUCE 首屏显示窗口标题、dirty 标记、路径提示和轨道数量，并提供最小可见按钮动作。
  - 根本原因：如果 JUCE 界面、菜单、快捷键或 AI 工具各自拼接工程状态，dirty 标记、未保存路径提示和轨道计数容易出现不一致。
  - 采用的解决方式：新增 `AppProjectStatus` 纯描述层，从 `AppProjectSession` 派生窗口标题、状态行、路径状态和轨道数量；JUCE 首屏只读取该快照刷新界面，按钮动作继续通过 `AppProjectSession` 修改当前工程。CTest 覆盖未保存 dirty 工程和已保存 clean 工程的状态描述。
  - 后续规则：后续菜单、快捷键、文件选择器、AI 工具或诊断面板展示当前工程状态时，应复用 `AppProjectStatus` 或等价单一描述入口；真正修改工程、保存或打开文件仍必须经过 `AppProjectSession`，不得在 UI 层复制 dirty/path 状态。
- 接入桌面工程文件选择器时：
  - 触发场景：JUCE 首屏需要提供打开工程、保存和另存为按钮，同时把保存失败、取消选择和无路径保存显示为用户能理解的状态。
  - 根本原因：如果 UI 直接解析 `AppProjectSessionResult::error` 文本或把取消选择当作错误，后续菜单、快捷键和 AI 工具会出现不同分支；如果未保存工程可直接新建或打开其他工程，用户容易误丢尚未保存的轨道修改。
  - 采用的解决方式：新增 `AppProjectFileActions`，用稳定 `AppProjectFileActionFeedbackKind` 区分成功、失败、取消和需要另存为；`AppProjectSessionResult` 增加稳定失败原因；JUCE `FileChooser` 由成员指针持有到异步回调结束；当前工程 dirty 时先提示保存或另存为，再允许新建或打开其他工程。CTest 覆盖默认 `.trackloom` 扩展名、无路径保存反馈、取消打开反馈和另存为成功反馈。
  - 后续规则：菜单、快捷键、AI 工具或自动恢复入口触发工程文件动作时，应复用 `AppProjectFileActions` 的反馈分类；取消选择不是错误；无路径保存必须引导另存为；任何会替换当前工程的动作都必须先处理 dirty 状态，直到实现正式确认弹窗前不得静默丢弃未保存修改。
- 管理桌面最近工程列表时：
  - 触发场景：阶段 C 需要为后续菜单、最近文件入口和本地设置存储准备最近工程列表，但当前还不应把它写进工程文件。
  - 根本原因：最近文件是某台电脑的本地使用偏好，不是音乐工程数据；如果混入 `Project` 或 `.trackloom` 文件，会污染可分享工程，也会给同步、备份和隐私边界制造歧义。
  - 采用的解决方式：新增 `AppRecentProjects` 应用层支持类，只记录本地最近工程路径，按最新在前去重和限长，并用 UTF-8 文本设置文件保存；再新增 `describeAppRecentProjects`、`recordAndSaveAppRecentProject` 和 `openAppRecentProjectByNumber`，让 JUCE 首屏显示、选择并打开最近工程，打开、保存和另存为成功后更新本地设置。CTest 覆盖去重限长、UTF-8 路径保存读取、缺失设置文件空列表、展示快照、设置保存失败时仍保留内存列表、按编号打开、dirty 阻断、缺失选择和缺失文件失败。
  - 后续规则：最近工程、窗口状态、UI 偏好等本地设置应放在应用层设置存储中，不进入核心 `Project` 格式；设置文件损坏或缺失不得阻止工程打开、编辑和保存；UI、菜单和诊断面板展示或打开最近工程时应复用描述快照和应用层打开入口，不要自行拼路径列表或绕过 dirty 保护。
- 建立桌面主菜单快照时：
  - 触发场景：首屏按钮已经覆盖新建、打开、保存、最近工程和播放控制后，需要开始接入正式菜单栏，同时避免菜单、按钮、快捷键和后续 AI 工具各自复制命令启用状态。
  - 根本原因：JUCE `MenuBarComponent` 只负责展示和分发菜单点击；如果菜单直接判断 `AppProjectSession`、`AppPlaybackController` 和 `AppRecentProjects` 的细节，后续命令面板、快捷键和诊断视图会出现不同的启用状态和最近工程编号规则。
  - 采用的解决方式：新增 `AppMainMenu` 应用层快照，输出“文件”“编辑”“轨道”和“播放”菜单项、稳定 command id、分隔线、空最近工程禁用行、最近工程动态 command id、撤销/重做启用状态、基础轨道创建命令和播放状态启用规则；JUCE 菜单栏读取快照渲染菜单，点击后继续调用现有动作函数。CTest 覆盖菜单分组、稳定 id、空最近工程行、撤销/重做历史状态、基础轨道创建项、播放中启用状态和最近工程 id 往返。
  - 后续规则：新增菜单项、快捷键、命令面板或 AI 命令入口时，应优先扩展 `AppMainMenu` 或等价应用层命令快照，再让 UI 绑定到现有动作；不要让菜单直接绕过 dirty 保护、最近工程打开入口、工程命令历史或播放控制动作。当前菜单只覆盖基础文件命令、基础撤销/重做、基础轨道创建命令和基础播放命令，不代表完整编辑菜单、完整轨道菜单、全局快捷键或正式未保存确认弹窗已经完成。
- 建立基础应用命令分发器时：
  - 触发场景：`AppMainMenu` 已能生成稳定 command id 后，JUCE `menuItemSelected` 仍直接 `switch` 到各个按钮函数，后续快捷键、命令面板或 AI 工具会再次复制这段映射。
  - 根本原因：菜单 id 解析、最近工程动态编号、缺失命令处理和具体 UI 动作耦合在 JUCE 类里，会让命令入口越来越难测试；同时最近工程动态 id 使用 `3000 + 编号`，测试未知命令时不能随便使用大于 3000 的数字。
  - 采用的解决方式：新增 `AppCommandDispatcher`，把普通菜单命令和最近工程动态命令解析成稳定 `AppCommandKind`，再调用注入的处理函数；2026-07-02 起，同一分发边界也覆盖基础撤销/重做和基础轨道创建命令。JUCE 菜单只构造回调表并调用分发器。CTest 覆盖普通命令只调用目标处理函数、撤销/重做分发、轨道创建分发、最近工程编号传递、未知 id 拒绝和已知命令缺少处理函数时的稳定失败结果。
  - 后续规则：菜单、快捷键、命令面板和 AI 工具新增入口时，应先进入应用层命令分发或等价边界，再调用具体动作；未知命令和缺失处理函数不得静默成功；测试未知命令 id 时要避开已定义的动态 id 区间。
- 建立命令面板数据层时：
  - 触发场景：文件、编辑、轨道和播放菜单已有稳定 command id 后，需要为后续命令面板、快捷键提示和 AI 工具提供同一份可搜索命令列表。
  - 根本原因：如果命令面板直接读取 JUCE 菜单或重新拼命令清单，会再次复制菜单分组、动态最近工程、禁用状态和 command id 规则；分隔线和“暂无最近工程”这类提示行也可能被误当作可执行命令。
  - 采用的解决方式：新增纯应用层 `AppCommandPalette`，从 `AppMainMenuStatus` 展开命令项，保留 group、label、enabled、command id 和可选 shortcut label，跳过分隔线与无 command id 的提示行，提供按标签、菜单组名或快捷键显示文本过滤的轻量搜索，并用 `selectAppCommandPaletteItem` 返回 `Selected`、`NoMatchingCommand` 或 `OnlyDisabledMatches` 三种稳定选择结果。确认执行时调用 `activateAppCommandPaletteCommand`，只有选中 enabled 命令才交给 `AppCommandDispatcher`，并把搜索失败、禁用匹配和分发失败分开返回。快捷键显示文本来自 `AppCommandShortcuts` 的默认绑定表，同一 command id 的多个快捷键会合并为一个展示标签，命令面板搜索也复用这些展示标签。CTest 先红后绿覆盖基础菜单展开、动态最近工程、禁用状态保留、轨道组搜索、提示行过滤、跳过 disabled 命令、缺失查询无选择、禁用匹配与无匹配分类、命令面板激活执行、禁用或无匹配不执行、分发失败分类、禁用命令仍显示快捷键、多个快捷键合并显示和快捷键文本搜索。
  - 后续规则：可见命令面板 UI、快捷键提示、AI 命令发现和诊断视图都应优先消费 `AppCommandPalette` 或等价应用层结果，再通过 `AppCommandDispatcher` 执行；不得让 UI 弹窗自行扫描 JUCE 菜单、复制命令列表、把非命令提示行当作可执行项，或绕过激活边界直接执行 disabled 命令。新增快捷键时必须先进入同一份默认快捷键绑定表，避免“可触发快捷键”“可显示快捷键”和“可搜索快捷键”三套数据漂移；命令面板反馈应读取稳定选择和激活结果枚举，不要通过空 optional 或显示文本猜测失败原因。
- 建立命令面板会话状态层时：
  - 触发场景：命令面板数据层已经能展开、过滤、选择和激活命令后，需要为后续可见弹窗准备打开/关闭、查询文本、过滤结果和当前高亮项，但还不应把这些状态散落到 JUCE 组件里。
  - 根本原因：如果 UI 自己保存查询和高亮索引，就会重复实现过滤、跳过 disabled 命令和循环导航规则；如果会话状态直接执行命令，又会混淆“浏览命令列表”和“确认执行命令”两个阶段。
  - 采用的解决方式：新增纯应用层 `AppCommandPaletteSession`，保存原始 palette 和只读状态快照；打开时清空 query 并高亮第一个 enabled 命令，查询变化时复用 `filterAppCommandPalette` 并重置高亮，上下移动只在 enabled 命令之间循环，关闭时清空过滤结果和高亮。随后新增 `activateHighlightedAppCommandPaletteCommand`，只执行当前高亮的 enabled 命令并复用 `AppCommandDispatcher`，关闭、无高亮、禁用匹配和分发失败都返回稳定结果。继续新增 `describeAppCommandPaletteSession`，把状态快照转换成 UI 可直接渲染的 rows、highlighted 标记和统一空结果提示。CTest 先红后绿覆盖打开初始状态、禁用项跳过、快捷键查询、禁用匹配无高亮、查询变化重置高亮、上下移动循环、关闭清空状态、高亮项执行、关闭或禁用状态不执行、缺失 handler 时报告分发失败、展示行高亮、禁用行和空结果提示。
  - 后续规则：可见命令面板 UI 应渲染 `describeAppCommandPaletteSession` 的结果，不要在 JUCE 组件里重新维护命令过滤、高亮索引、disabled 跳过、空结果提示或快捷键行文案；按 Enter 或点击确认时应执行当前高亮项，不得重新按 query 选择第一条命令；执行确认仍应走已有激活或分发边界，会话状态层不得直接调用具体工程动作。
- 建立第一批桌面文件快捷键时：
  - 触发场景：基础文件/播放菜单和 `AppCommandDispatcher` 已存在后，需要让常见文件快捷键复用同一命令 id 和执行边界，而不是在 JUCE `keyPressed` 里重新写保存、打开和新建逻辑。
  - 根本原因：快捷键组合、平台主修饰键和命令执行是不同层次；如果直接在 JUCE 层判断 `Ctrl+S` 后调用保存函数，后续菜单、快捷键、命令面板和 AI 工具会逐渐分叉。Space 播放/停止是切换语义，不等同于菜单里的“播放”和“停止”两个独立命令，不能硬塞进同一批文件快捷键映射。
  - 采用的解决方式：新增平台无关的 `AppCommandShortcuts`，把 `primaryModifier + N/O/S/Shift+S` 映射到 `AppMainMenuCommand` 的稳定 command id；2026-07-02 起，继续把 `primaryModifier + Z/Y/Shift+Z` 映射到撤销/重做 command id。JUCE 只把 `KeyPress` 转成 `AppShortcutChord`，再交给 `dispatchAppCommand` 执行。CTest 覆盖 `Ctrl+N`、`Ctrl+O`、`Ctrl+S`、`Ctrl+Shift+S`、`Ctrl+Z`、`Ctrl+Y`、`Ctrl+Shift+Z` 和无主修饰键、带 Alt、未注册字符的拒绝。
  - 后续规则：新增快捷键时应先扩展平台无关映射并测试，再让 JUCE 或其他前端转换输入事件；不得让快捷键绕过 dirty 保护、文件选择反馈、工程命令历史或命令分发器；播放/停止切换继续使用 `toggleAppPlayback`，除非后续明确新增独立 Toggle 命令并补测试。
- 建立应用层工程命令历史边界时：
  - 触发场景：核心 `CommandStack` 已能撤销/重做，但桌面首屏动作长期通过 `AppProjectSession::editProject()` 直接修改工程；如果直接暴露撤销菜单，可能跳过未记录的片段或音符编辑，误撤更早的轨道操作。
  - 根本原因：命令历史只能理解通过 `Command` 执行的编辑，无法自动知道旧直接编辑入口做过什么；混用两种修改路径时，保留旧历史比没有撤销更危险。
  - 采用的解决方式：在 `AppProjectSession` 新增 `executeProjectCommand`、`undoProjectEdit`、`redoProjectEdit` 和历史查询入口；成功命令才标脏并进入历史。直接 `editProject()` 会清空旧历史，表示这次修改没有可撤销记录。首批迁移 `AppTrackActions` 的轨道创建、删除、重命名和移动到核心命令执行；随后迁移 `AppTrackStateActions` 的静音、独奏、禁用和隐藏到核心状态命令，迁移 `AppMidiClipActions` 的默认 MIDI 片段创建、删除、重命名、复制、中点拆分、一拍左右移动、跨轨移动、片尾缩短/延长和片头缩短/延长到核心片段命令，迁移 `AppAudioClipActions` 当前已有的空音频片段外壳动作到核心片段命令，并迁移 `AppMidiNoteActions` 的默认音符创建、末尾音符删除、末尾音符复制、音高、力度、长度和起点微调到核心音符命令。CTest 覆盖成功命令撤销/重做、失败命令不标脏不入栈、新建工程清空历史、直接编辑清空历史，以及轨道创建、静音、隐藏、MIDI 片段创建/删除/重命名/复制/拆分/一拍移动/跨轨移动/片尾缩短/延长/片头缩短/延长、音频片段创建/删除/重命名/复制/拆分/跨轨移动/一拍移动/片尾缩短/片尾延长/片头缩短/片头延长、MIDI 音符创建/删除/复制/音高/力度/长度/起点微调可通过会话历史撤销/重做。
  - 后续规则：新增或迁移编辑入口时优先通过 `executeProjectCommand` 或等价可撤销边界执行；基础编辑菜单可以触发当前会话历史撤销/重做，但在后续新增编辑入口全部迁移并补齐快捷键、命令面板和用户可见失败反馈前，不应宣称完整 Undo/Redo 体验；任何仍需直接 `editProject()` 的过渡代码必须清空命令历史，避免跨未记录编辑撤销。
- 接入桌面撤销/重做菜单时：
  - 触发场景：轨道、片段和 MIDI 音符当前按钮动作已进入 `AppProjectSession` 命令历史后，需要给用户可见的撤销/重做入口，而不是只在测试里调用会话 API。
  - 根本原因：如果 JUCE 菜单直接调用 `session_.undoProjectEdit()`，菜单启用状态、命令 id、快捷键和后续命令面板会各自复制一套规则；如果把播放、最近工程或文件选择器状态放进工程撤销栈，会混淆运行态和工程编辑。
  - 采用的解决方式：扩展 `AppMainMenu` 增加“编辑”组和稳定 `UndoProject`/`RedoProject` command id，启用状态只读取 `canUndoProjectEdit()`/`canRedoProjectEdit()`；扩展 `AppCommandDispatcher` 通过注入回调执行撤销/重做；JUCE 只绑定 handler 并刷新提示。CTest 覆盖编辑菜单项、历史状态启用、撤销/重做分发。
  - 后续规则：所有用户可见撤销/重做入口都必须经同一 command id 和分发器；工程撤销/重做只处理 `Project` 命令历史，不包含播放头、最近工程、文件选择器、设置或其他运行态。
- 接入桌面撤销/重做快捷键时：
  - 触发场景：基础“编辑”菜单已能触发工程撤销/重做后，需要让常见键盘操作进入同一命令路径。
  - 根本原因：如果在 JUCE `keyPressed` 里直接调用 `undoProjectEdit()` 或 `redoProjectEdit()`，菜单、快捷键和后续命令面板会分裂成多套入口；如果只支持一种重做组合，也会和 Windows/常见创作软件中的 `Ctrl+Y`、`Ctrl+Shift+Z` 习惯不一致。
  - 采用的解决方式：扩展 `AppCommandShortcuts`，让 `Ctrl+Z` 映射 `UndoProject`，`Ctrl+Y` 和 `Ctrl+Shift+Z` 映射 `RedoProject`；JUCE 仍只负责把按键转换为 `AppShortcutChord` 并交给命令分发器。CTest 先红后绿覆盖三种组合。
  - 后续规则：撤销/重做快捷键不得绕过 `AppMainMenuCommand` 和 `AppCommandDispatcher`；未来增加平台原生菜单快捷键展示、自定义快捷键或命令面板时，必须复用同一 command id，不能另写直接会话调用。
- 接入基础轨道菜单时：
  - 触发场景：首屏已经有添加乐器轨、添加音频轨和添加文件夹轨按钮后，需要把不依赖当前选择的轨道创建动作接入菜单和统一命令分发。
  - 根本原因：轨道创建不需要读取 UI 选择状态，适合先接菜单；删除、重命名、移动和片段编辑依赖当前目标轨道或片段，如果过早塞进菜单描述层，会迫使 `AppMainMenu` 读取 JUCE 私有选择状态。
  - 采用的解决方式：扩展 `AppMainMenu` 新增“轨道”组和稳定 `AddInstrumentTrack`、`AddAudioTrack`、`AddFolderTrack` command id；扩展 `AppCommandDispatcher` 注入三个 handler；JUCE 继续复用已有 `addDefaultInstrumentTrack`、`addDefaultAudioTrack` 和 `addDefaultFolderTrack`。CTest 先红后绿覆盖轨道菜单项和三种分发。
  - 后续规则：无选择依赖的菜单命令可以直接进入应用层快照；选择依赖命令必须先设计可测试的选择状态输入，不得让菜单层读取 JUCE 控件状态或绕过 `AppTrackActions`。
- 展示桌面轨道列表时：
  - 触发场景：JUCE 首屏已有“添加乐器轨”按钮后，需要让用户看到真实轨道行，而不是只看到轨道数量。
  - 根本原因：如果界面直接遍历 `Project::tracks()` 并自行解释轨道类型、片段数量和状态标签，后续轨道选择、时间线、设备路由、AI 工具和诊断面板容易出现显示规则不一致。
  - 采用的解决方式：新增 `AppTrackListStatus` 只读描述层，把 `Project` 轨道转换为编号、名称、类型、片段数量和状态标签；JUCE 首屏只渲染该快照，不在 UI 层重新解释 `TrackType` 或播放/显示状态。CTest 覆盖空工程、轨道顺序、类型标签、片段计数和静音/独奏/禁用/隐藏状态。
  - 后续规则：后续轨道列表、时间线左侧轨道头、设备路由面板或 AI 诊断展示轨道信息时，应复用 `AppTrackListStatus` 或等价单一描述入口；内部 track id 可用于逻辑选择，但默认不要暴露给普通用户界面。
- 管理桌面乐器轨增删入口时：
  - 触发场景：JUCE 首屏已有轨道列表、目标乐器轨选择和 MIDI 片段创建入口后，需要把“添加乐器轨”的直接工程调用收回应用层，并提供删除目标乐器轨能力。
  - 根本原因：如果 UI 直接调用 `Project::createTrack` 或 `Project::removeTrackById`，会把默认命名、目标轨道类型校验、dirty 状态和反馈文案分散到界面层；后续菜单、快捷键和 AI 工具也会重复这些规则。
  - 采用的解决方式：新增 `AppTrackActions`，负责默认 `Instrument N` 命名、默认乐器轨创建、目标乐器轨删除、缺失轨道拒绝和非乐器轨拒绝；JUCE 首屏只保存内部 track id 并通过动作层修改工程。CTest 覆盖默认创建、连续命名、删除乐器轨及其片段/音符、缺失轨道删除拒绝和非乐器轨删除拒绝。
  - 后续规则：后续菜单、快捷键、AI 工具或轨道头按钮创建/删除乐器轨时，应复用 `AppTrackActions` 或等价应用层入口；失败校验不得触碰 `AppProjectSession::editProject()`，避免把未修改工程标脏；当前入口只代表乐器轨，不要把它误扩展成通用轨道删除体验。
- 创建和删除桌面空音频轨入口时：
  - 触发场景：首屏轨道列表已能显示不同轨道类型后，需要先提供音频轨外壳，为后续音频文件导入、波形和音频片段播放打底；在空音频片段外壳可创建后，也需要能删除目标音频轨并清理该轨拥有的片段。
  - 根本原因：音频轨创建/删除和音频素材导入是不同能力；如果一次性把“添加音频轨”做成导入音频文件，或让 UI 直接调用 `removeTrackById`，会过早混合轨道模型、文件选择、素材管理、波形、播放、缺失素材错误处理、dirty 状态和错误文案。当前目标乐器轨下拉框也只能承载 MIDI 片段创建，不应把音频轨塞进去。
  - 采用的解决方式：扩展 `AppTrackActions`，新增 `createDefaultAudioTrack` 和 `deleteAudioTrackById`，只创建 `Audio N` 空音频轨，或只删除 `TrackType::Audio` 轨道并让工程模型移除其拥有的片段；JUCE 首屏新增“添加音频轨”和“删除音频轨”按钮，音频轨删除成功后清理旧音频轨/音频片段选择。CTest 覆盖成功创建音频轨、连续音频轨命名、成功删除音频轨及其片段、缺失音频轨删除拒绝、非音频轨删除拒绝和 dirty 状态。
  - 后续规则：音频文件导入、音频片段创建、波形显示、音频播放和完整轨道类型选择器必须作为独立步骤设计和测试；目标乐器轨选择继续只包含乐器轨，不能因为存在音频轨而允许创建 MIDI 片段到音频轨；删除音频轨入口必须继续只接受音频轨，失败校验不得触碰 `AppProjectSession::editProject()`。
- 创建桌面空文件夹轨入口时：
  - 触发场景：乐器轨和空音频轨外壳已经能在首屏创建后，需要补齐首期第三类轨道外壳，同时不提前承诺文件夹层级和折叠编辑。
  - 根本原因：文件夹轨外壳、层级归组、折叠显示、子轨道批量移动和文件夹轨删除是不同能力；如果创建入口直接暗含层级语义，会让 UI、工程模型和后续测试过早承担尚未定义的父子关系规则。
  - 采用的解决方式：扩展 `AppTrackActions`，新增 `createDefaultFolderTrack`，只创建 `Folder N` 空文件夹轨并返回稳定反馈；JUCE 首屏新增“添加文件夹”按钮，创建后只刷新轨道列表和消息，不改变目标乐器轨选择。CTest 覆盖成功创建文件夹轨、连续文件夹轨命名和 dirty 状态。
  - 后续规则：文件夹层级、折叠显示、轨道归组、批量移动、文件夹轨删除和完整轨道类型选择器必须作为独立步骤设计和测试；目标乐器轨选择继续只包含乐器轨，不能因为存在文件夹轨而允许创建 MIDI 片段到文件夹轨。
- 管理桌面轨道重命名入口时：
  - 触发场景：首屏可以选择目标乐器轨后，需要允许用户把默认 `Instrument N` 改成可读名称，同时保持轨道列表和时间线摘要一致。
  - 根本原因：如果 UI 直接调用 `Project::renameTrackById`，空名称校验、首尾空白清理、dirty 状态和反馈文案会散落在界面层；如果界面刷新时总是把文本框重置为工程名称，播放 Timer 或状态刷新会覆盖用户正在输入的内容。
  - 采用的解决方式：扩展 `AppTrackActions`，新增 `renameTrackById` 应用层入口，统一清理名称、拒绝空名称和缺失轨道；JUCE 首屏只有在选择变化或输入框未聚焦时同步工程名称，按按钮或回车才提交重命名。CTest 覆盖成功重命名、空白清理、空名称拒绝、缺失轨道拒绝和轨道列表新名称。
  - 后续规则：菜单、快捷键、轨道头、属性面板和 AI 工具重命名轨道时，应复用 `AppTrackActions` 的重命名入口；输入控件刷新必须尊重用户正在编辑的焦点状态，避免后台 Timer 或状态刷新覆盖未提交文本。
- 管理桌面轨道顺序调整时：
  - 触发场景：首屏轨道列表和目标轨道下拉框已经能显示项目顺序后，需要让用户用按钮调整当前乐器轨的前后位置。
  - 根本原因：如果 UI 直接调用 `Project::moveTrackToIndex`，顶部/底部边界、目标类型校验、dirty 状态和反馈文案会分散到界面层；如果移动后按显示行号保持选择，会在顺序变化后错误选中相邻轨道。
  - 采用的解决方式：扩展 `AppTrackActions`，新增 `moveInstrumentTrackUp` 和 `moveInstrumentTrackDown`，在 `editProject()` 前完成缺失轨道、非乐器轨和顶部/底部校验；JUCE 首屏移动后继续按稳定 track id 保持当前选择。CTest 覆盖上移、下移、边界拒绝、缺失轨道拒绝和非乐器轨拒绝。
  - 后续规则：菜单、快捷键、轨道头、拖拽排序和 AI 工具调整轨道顺序时，应复用应用层移动入口或等价边界；移动后选择应跟随稳定 track id，而不是跟随列表行号。
- 管理桌面轨道状态切换时：
  - 触发场景：首屏轨道列表已经能展示静音、独奏、禁用和隐藏状态后，需要让用户能从当前选中轨道切换这些状态。
  - 根本原因：如果 UI 直接复制 `TrackPlaybackState` 或 `TrackViewState` 修改规则，静音、独奏、禁用、隐藏会被不同入口互相覆盖；失败校验也容易在没有真实修改时误触发 dirty 状态。
  - 采用的解决方式：新增 `AppTrackStateActions`，把轨道状态切换收敛成静音、独奏、禁用、隐藏四个应用层入口；每次只翻转一个字段并保留其他状态，JUCE 首屏只负责传入当前选中 track id。2026-07-01 起，状态切换通过 `SetTrackPlaybackStateCommand` 和 `SetTrackViewStateCommand` 写入工程，进入 `AppProjectSession` 命令历史。CTest 覆盖成功切换标脏、播放状态互不覆盖、隐藏不影响播放状态、缺失轨道不标脏不入历史、列表状态摘要，以及静音和隐藏可通过会话历史撤销/重做。
  - 后续规则：菜单、快捷键、轨道头、混音器和 AI 工具切换轨道播放/显示状态时，应复用 `AppTrackStateActions` 或等价应用层入口；显示状态不能承担播放语义，隐藏不得隐式静音，失败校验不得触碰 `editProject()`；新增状态动作应优先选用对应核心 `Command`，不能绕过命令历史。
- 创建和删除桌面 MIDI 片段入口时：
  - 触发场景：JUCE 首屏已有轨道列表后，需要让用户选择乐器轨创建第一个可保存的 MIDI 片段，也需要能从首屏删除目标 MIDI 片段，并同步显示时间线摘要。
  - 根本原因：如果 UI 直接调用 `Project::createClip` 或 `Project::removeClipById`，会把目标轨道/片段校验、默认片段长度、追加位置、dirty 状态和错误文案分散到界面层；后续菜单、快捷键和 AI 工具也会重复这些规则。
  - 采用的解决方式：新增并扩展 `AppMidiClipActions` 负责目标轨道校验、默认 MIDI 片段创建、目标 MIDI 片段删除、目标 MIDI 片段重命名、目标 MIDI 片段复制和目标 MIDI 片段中点拆分，新增 `AppTimelineStatus` 负责只读时间线摘要；JUCE 首屏只保存当前选择的内部 track id 和 clip id，不向普通用户暴露这些 id。2026-07-01 起，默认 MIDI 片段创建通过 `AddClipCommand` 执行，删除通过 `DeleteClipCommand` 执行，重命名通过 `RenameClipCommand` 执行，复制通过 `DuplicateClipCommand` 执行，拆分通过 `SplitClipCommand` 执行，因此创建、删除、重命名、复制、拆分、撤销和重做都能保持稳定 clip id，并能恢复被删片段内的 MIDI 音符、旧名称、复制得到的新 note id 或拆分得到的右侧片段与移动后的 MIDI 音符。CTest 覆盖成功创建、追加位置、缺失轨道、非乐器轨拒绝、成功删除、缺失片段删除拒绝、音频片段删除拒绝、空时间线、片段行摘要，以及创建/删除/重命名/复制/拆分可通过会话历史撤销/重做。
  - 后续规则：后续菜单、快捷键、AI 工具或时间线按钮创建/删除默认 MIDI 片段时，应复用 `AppMidiClipActions` 或等价应用层入口；时间线展示应复用 `AppTimelineStatus`；失败校验不得触碰 `AppProjectSession::editProject()`，避免把未修改工程标脏；新增 MIDI 片段编辑能力应优先选用已有核心片段命令并补撤销/重做测试。
- 创建、重命名、复制、拆分、移动、片头/片尾编辑和删除桌面音频片段外壳时：
  - 触发场景：首屏已经能创建空音频轨后，需要让音频轨在时间线中拥有可保存、可展示、可命名、可复制、可拆分、可移动到另一条音频轨、可移除的最小片段对象，但真实音频文件导入、波形和播放尚未实现。
  - 根本原因：如果复用“目标乐器轨”选择器、MIDI 片段动作、MIDI 名称输入框或 UI 直接调用 `Project::createClip` / `Project::renameClipById` / `Project::duplicateClipToTrackAtTick` / `Project::splitClipAtTick` / `Project::moveClipToTrack` / `Project::setClipTiming` / `Project::trimClipEndToTick` / `Project::removeClipById`，音频轨和乐器轨的类型边界、追加位置、复制起点、拆分切点、跨轨目标、名称清理、左边界、最短长度、dirty 状态和错误文案会分散并漂移。
  - 采用的解决方式：新增并扩展 `AppAudioClipActions`，只允许在 `TrackType::Audio` 上追加默认一小节空音频片段，只允许重命名、复制到同轨道源片段结束位置、从长度中点拆分成左右两个空音频片段、移动到另一条音频轨、按一拍左右移动、按一拍缩短/延长片头和片尾，以及删除 `ClipType::Audio` 片段；JUCE 首屏新增独立“目标音频轨”和“目标音频片段”选择器，以及独立音频片段名称输入框，创建、复制和拆分后选中新片段，跨轨移动成功后保留被移动片段并选择目标音频轨，删除后清空旧选择，切换音频片段时同步音频名称输入框，复制、拆分、跨轨移动、一拍移动和片头/片尾编辑按钮只调用应用层动作。`AppTimelineStatus` 空提示同步改成 MIDI/音频片段通用提示。CTest 覆盖成功创建、连续追加、缺失轨道拒绝、非音频轨拒绝、成功重命名、空名称拒绝、缺失片段重命名拒绝、MIDI 片段重命名拒绝、成功复制、缺失片段复制拒绝、MIDI 片段复制拒绝、成功中点拆分、缺失片段拆分拒绝、MIDI 片段拆分拒绝、过短音频片段拆分拒绝、成功跨音频轨移动、同轨移动拒绝、缺失源片段跨轨移动拒绝、缺失目标轨跨轨移动拒绝、乐器目标轨跨轨移动拒绝、MIDI 片段跨轨移动拒绝、成功左右移动、左边界拒绝、缺失片段移动拒绝、MIDI 片段移动拒绝、成功片尾缩短和延长、过短片段片尾缩短拒绝、缺失片段片尾编辑拒绝、MIDI 片段片尾编辑拒绝、成功删除、缺失片段删除拒绝和 MIDI 片段删除拒绝。
  - 后续规则：音频片段创建、重命名、复制、拆分、跨轨移动、一拍移动、片头/片尾编辑、删除、导入、真实修剪和播放必须继续区分音频轨与乐器轨；失败校验不得触碰 `AppProjectSession::editProject()`，避免把未修改工程标脏；音频片段 UI 不得用 MIDI 片段选择或 MIDI 名称输入框承担逻辑；未接入素材引用和音频引擎前，只能称为空音频片段外壳，片段复制只能称为外壳复制，片段拆分只能称为外壳拆分，片段跨轨移动只能称为外壳归属切换，片段一拍移动只能称为外壳起点移动，片尾缩短/延长只能称为外壳长度编辑，片头缩短/延长只能称为外壳左边界编辑，不得宣称已经支持音频导入、素材复制、素材移动、素材偏移、波形显示、可听播放、真实音频切点、交叉淡化、拖拽定位、吸附网格、重叠处理或真实音频修剪。
- 迁移桌面音频片段外壳动作到命令历史时：
  - 触发场景：空音频片段创建、重命名、删除、复制、拆分、跨轨移动、一拍移动和片头/片尾缩短延长已经可用后，需要让这些用户动作都能通过会话历史撤销/重做，而不是继续绕过上一阶段建立的 `AppProjectSession` 命令栈。
  - 根本原因：`session.editProject()` 会直接修改工程并清空旧历史；如果音频片段动作继续直接调用 `Project`，撤销菜单未来会跳过这些用户可见编辑，或只能放弃完整撤销。
  - 采用的解决方式：将 `createDefaultAudioClipOnTrack` 改为执行 `AddClipCommand`，`renameAudioClipById` 改为执行 `RenameClipCommand`，`deleteAudioClipById` 改为执行 `DeleteClipCommand`，`duplicateAudioClipAfterItself` 改为执行 `DuplicateClipCommand`，`splitAudioClipAtMidpoint` 改为执行 `SplitClipCommand`，`moveAudioClipToTrack` 改为执行 `MoveClipToTrackCommand`，一拍移动、片尾延长和片头延长改为执行 `SetClipTimingCommand`，片尾缩短改为执行 `TrimClipEndCommand`，片头缩短改为执行 `TrimClipStartCommand`。创建、复制和拆分都用执行前后的 clip id 差集找回新片段反馈给 UI。CTest 覆盖这些当前音频片段外壳动作的撤销/重做，并确认重做保留同一个 clip id、名称、轨道和时间范围。
  - 后续规则：新增音频片段动作时，必须先补红测覆盖撤销/重做，再改为核心命令或新增原子命令；一次用户动作不能拆成多次用户可见撤销。失败路径继续在只读校验阶段返回，不得触碰 `editProject()`。当前迁移只覆盖空音频片段外壳动作，不代表音频文件导入、素材偏移、真实修剪、波形或可听播放已经实现。
- 编辑桌面空音频片段片头时：
  - 触发场景：空音频片段外壳已有创建、移动和片尾长度编辑后，需要补齐固定一拍的片头缩短/延长按钮，但音频文件导入、素材引用和素材偏移还没有实现。
  - 根本原因：当前 `TimelineClip` 只保存时间线外壳的起点和长度；真实音频素材片头修剪必须知道素材文件、素材内偏移、淡化、尾音和重叠策略，不能直接套用 MIDI 片头修剪的音符迁移规则。
  - 采用的解决方式：扩展 `AppAudioClipActions`，新增 `trimAudioClipStartLaterOneBeat` 和 `extendAudioClipStartEarlierOneBeat`，只允许 `ClipType::Audio` 且所属轨道为 `TrackType::Audio` 的空外壳执行；片头缩短通过 `TrimClipStartCommand` 保持旧终点稳定，片头延长通过 `SetClipTimingCommand` 在左侧增加空外壳长度，并进入 `AppProjectSession` 命令历史；JUCE 首屏新增“缩短音频片头”和“延长音频片头”按钮，并把窗口高度调高避免控件拥挤。CTest 覆盖成功缩短、成功延长、过短拒绝、越过时间线起点拒绝、缺失片段拒绝、MIDI 片段拒绝和撤销/重做。
  - 后续规则：在素材引用、素材内偏移和真实音频修剪规则完成前，音频片头编辑只能描述为空外壳左边界编辑；后续接入真实素材时必须重新设计素材 offset、波形、可听播放、交叉淡化和重叠冲突测试，不能把当前外壳逻辑当成素材修剪实现。
- 展示桌面 MIDI 片段末尾音符详情时：
  - 触发场景：首屏已经能调整末尾 MIDI 音符的音高、力度、长度、起点和复制结果后，时间线仍只显示音符数量，用户无法从首屏确认按钮操作到底改了什么。
  - 根本原因：如果摘要层另写一套“最后一个音符”规则，可能和 `AppMidiNoteActions` 实际编辑的目标音符不一致；如果只把细节写在 JUCE 文本拼接里，后续测试、快捷键反馈和诊断面板难以复用。
  - 采用的解决方式：扩展 `AppTimelineStatus`，在 `AppTimelineClipRow` 中加入结构化末尾音符字段，并用与音符编辑相同的“更晚结束 tick，随后更晚开始 tick”规则填充；摘要中显示末尾音符起点、长度、音高和力度。CTest 覆盖有音符时显示详情、空 MIDI 片段不显示详情。
  - 后续规则：任何首屏或诊断视图展示“当前编辑目标”时，必须复用与实际编辑动作一致的选择规则；展示状态优先放在应用层值类型快照中，再由 UI 渲染，不要让界面文本成为唯一数据来源。
- 调试 Windows/MSVC Debug 测试可执行文件卡住时：
  - 触发场景：修改 `trackloom_app_support_tests` 的断言后，直接运行测试可执行文件没有输出且长期不退出，进程停留在 `trackloom_app_support_tests.exe`。
  - 根本原因：失败的 `require` 会抛出未捕获异常；MSVC Debug 运行时可能弹出或等待调试错误处理，终端侧看起来像测试死循环。
  - 采用的解决方式：用 `Get-Process trackloom_app_support_tests` 找到卡住进程并 `Stop-Process -Force` 结束；检查最近测试 diff，确认是刚改的空时间线文案断言失败；更新 `AppTimelineStatus` 文案后重新运行测试通过。
  - 后续规则：Windows 下测试可执行文件无输出卡住时，先确认是否是断言失败触发 Debug 运行时等待，不要直接猜成业务逻辑死循环；结束卡住进程后按最近测试改动和预期红灯结果定位。
- 复制桌面 MIDI 片段入口时：
  - 触发场景：首屏已经能创建、选择和删除 MIDI 片段后，需要给用户一个不用重新录入音符的最小复制能力。
  - 根本原因：如果 UI 直接调用 `Project::duplicateClipToTrackAtTick`，缺失片段、音频片段、目标轨道类型、dirty 状态和反馈文案会分散到界面层；如果复制 MIDI 音符时复用原 note id，后续命令系统会把源片段和副本里的音符误认为同一对象。
  - 采用的解决方式：扩展 `AppMidiClipActions`，新增 `duplicateMidiClipAfterItself`，只接受 MIDI 片段，把副本放到同轨道源片段结束位置，并依赖核心复制逻辑为副本音符分配新 ID；JUCE 首屏复制成功后选中新片段。CTest 覆盖成功复制、缺失片段拒绝和音频片段拒绝。
  - 后续规则：菜单、快捷键、AI 工具、时间线拖拽复制或剪贴板复制 MIDI 片段时，应复用应用层复制入口或等价边界；副本中的音符和未来自动化事件必须拥有新 ID，不得与源片段共享对象身份。
- 重命名桌面 MIDI 片段入口时：
  - 触发场景：首屏已经能选择目标 MIDI 片段后，需要允许用户把默认片段名改成可读名称，并让时间线摘要同步显示。
  - 根本原因：如果 UI 直接调用 `Project::renameClipById`，空名称校验、首尾空白清理、MIDI/音频片段边界、dirty 状态和反馈文案会散落在界面层；如果刷新时总把片段名写回输入框，会覆盖用户正在输入的未提交名称。
  - 采用的解决方式：扩展 `AppMidiClipActions`，新增 `renameMidiClipById`，统一清理名称、拒绝空名称、缺失片段和音频片段；JUCE 首屏只有在选择变化或输入框未聚焦时同步工程片段名，按按钮或回车才提交重命名。CTest 覆盖成功重命名、空白清理、空名称拒绝、缺失片段拒绝、音频片段拒绝和时间线新名称。
  - 后续规则：菜单、快捷键、时间线内联编辑、属性面板和 AI 工具重命名 MIDI 片段时，应复用 `AppMidiClipActions` 的重命名入口；输入控件刷新必须尊重用户正在编辑的焦点状态，避免后台 Timer 或状态刷新覆盖未提交文本。
- 拆分桌面 MIDI 片段入口时：
  - 触发场景：首屏已经能选择和复制 MIDI 片段后，需要提供一个不依赖拖拽坐标的最小拆分能力。
  - 根本原因：核心拆分会拒绝跨切点 MIDI 音符；如果应用层直接调用 `editProject().splitClipAtTick` 再让核心失败，工程虽然未变但会被会话误标脏。跨切点音符也不能被界面层静默切断，否则会无提示改写用户演奏。
  - 采用的解决方式：扩展 `AppMidiClipActions`，新增 `splitMidiClipAtMidpoint`，先验证目标片段存在、类型为 MIDI、片段有有效中点，并预检没有音符跨越中点；成功拆分通过 `SplitClipCommand` 进入 `AppProjectSession` 命令历史，失败路径仍在 `editProject()` 前返回。JUCE 首屏拆分成功后选中新生成的右侧片段。CTest 覆盖成功拆分、右侧音符移动、缺失片段拒绝、音频片段拒绝、过短片段拒绝、跨中点音符拒绝，以及拆分可通过会话历史撤销/重做。
  - 后续规则：菜单、快捷键、刀片工具、AI 工具或时间线任意切点拆分 MIDI 片段时，必须先预检跨切点音符策略；没有明确拆音、延音或截断规则前，不得静默改写跨切点音符，失败也不得触碰 `editProject()`。
- 移动桌面 MIDI 片段入口时：
  - 触发场景：首屏已有 MIDI 片段复制、重命名和拆分后，需要提供不依赖拖拽坐标的最小位置调整能力。
  - 根本原因：核心 `setClipTiming` 只验证时间合法性和音符是否仍在片段内，不负责应用层的“只移动 MIDI 片段”“一拍步长”“左边界反馈”和 dirty 语义；如果 UI 直接调用可编辑工程，左移越界、缺失片段或音频片段失败会误标 dirty。
  - 采用的解决方式：扩展 `AppMidiClipActions`，新增 `moveMidiClipLeftOneBeat` 和 `moveMidiClipRightOneBeat`，先用只读工程快照验证目标片段存在、类型为 MIDI、所属轨道仍为乐器轨、目标起点不在 0 之前，再通过 `SetClipTimingCommand` 只移动片段起点并进入命令历史。JUCE 首屏新增“左移片段”和“右移片段”按钮。CTest 覆盖右移、左移、左边界拒绝、缺失片段拒绝、音频片段拒绝和一拍移动撤销/重做。
  - 后续规则：菜单、快捷键、AI 工具、时间线拖拽和吸附移动应复用应用层移动边界或等价校验；移动片段默认不得改写片段内部 MIDI 音符相对 tick；失败路径不得触碰 `editProject()`；重叠冲突、吸附网格和跨轨移动必须作为单独规则测试。
- 跨轨移动桌面 MIDI 片段入口时：
  - 触发场景：首屏已有片段左右移动后，需要把 MIDI 片段从一条乐器轨移动到另一条乐器轨，补齐最小时间线编辑能力。
  - 根本原因：核心 `moveClipToTrack` 会验证目标轨能否拥有该片段，但它允许同轨赋值；`AppProjectSession::editProject()` 会先标脏，如果 UI 直接调用，同轨 no-op、缺失目标轨或类型不兼容都会把未变工程误标 dirty。
  - 采用的解决方式：扩展 `AppMidiClipActions`，新增 `moveMidiClipToTrack`，先验证源片段存在、类型为 MIDI、源轨和目标轨都是乐器轨、目标轨不同于当前轨，再通过 `MoveClipToTrackCommand` 进入 `AppProjectSession` 命令历史。JUCE 首屏新增“移到目标轨”按钮，复用已有目标乐器轨和目标 MIDI 片段选择。CTest 覆盖成功跨轨移动、同轨拒绝、缺失目标轨拒绝、音频目标轨拒绝、音频片段拒绝和跨轨移动撤销/重做。
  - 后续规则：拖拽跨轨、菜单、快捷键、AI 工具和批量移动必须复用应用层跨轨移动边界或等价校验；同轨移动应视为无效操作且不得标脏；跨轨移动不得改变片段时间范围或内部 MIDI 音符相对 tick；重叠冲突、跨类型转换和批量移动必须另行设计并测试。
- 修剪桌面 MIDI 片段片尾入口时：
  - 触发场景：首屏已有 MIDI 片段移动后，需要提供一个最小的时间范围缩短能力，为后续拖拽修剪打底。
  - 根本原因：核心 `trimClipEndToTick` 会拒绝无效长度和超出片段的新音符范围，但 `AppProjectSession::editProject()` 会先标脏；如果应用层不预检片段长度和音符边界，过短片段或会截掉音符的修剪会把未变工程误标 dirty。当前也没有素材偏移模型，不能把片尾修剪扩展成左边界修剪或向外扩展。
  - 采用的解决方式：扩展 `AppMidiClipActions`，新增 `trimMidiClipEndEarlierOneBeat`，先验证目标片段存在、类型为 MIDI、所属轨道仍为乐器轨、长度超过一拍，并确认缩短后不会让任何 MIDI 音符超出新片段长度，再通过 `TrimClipEndCommand` 进入 `AppProjectSession` 命令历史。JUCE 首屏新增“缩短片尾”按钮。CTest 覆盖成功缩短、过短拒绝、缺失片段拒绝、音频片段拒绝、会截掉音符的修剪拒绝和片尾缩短撤销/重做。
  - 后续规则：菜单、快捷键、AI 工具和时间线拖拽修剪必须在应用层明确素材偏移、音符截断、延音和 dirty 语义；没有这些规则前，只允许向内缩短不会丢音符的片尾，失败路径不得触碰 `editProject()`。
- 延长桌面 MIDI 片段片尾入口时：
  - 触发场景：首屏已有片尾缩短后，需要提供一个不会改变已有音符位置的最小片段扩展能力。
  - 根本原因：左边界修剪需要明确素材偏移和 MIDI 音符相对 tick 迁移规则；在这些原子规则缺失前直接接 UI 容易意外移动或裁掉音符。右边界延长只增加空白时间，但仍需要应用层预检目标片段、轨道类型、时间溢出和 dirty 语义。
  - 采用的解决方式：扩展 `AppMidiClipActions`，新增 `extendMidiClipEndLaterOneBeat`，先用只读快照验证目标片段存在、类型为 MIDI、所属轨道仍为乐器轨，并确认新长度和新片尾不溢出，再通过 `SetClipTimingCommand` 只增加片段长度并进入命令历史。JUCE 首屏新增“延长片尾”按钮。CTest 覆盖成功延长、缺失片段拒绝、音频片段拒绝和片尾延长撤销/重做。
  - 后续规则：菜单、快捷键、AI 工具和时间线拖拽扩展应复用应用层右边界延长规则或等价校验；延长片尾不得移动片段起点或内部 MIDI 音符相对 tick；左边界修剪必须先设计并测试素材偏移、音符迁移和冲突处理。
- 修剪和延长桌面 MIDI 片段片头入口时：
  - 触发场景：片尾缩短/延长已经可用后，需要补齐固定一拍的左边界编辑能力，同时不能让片头操作意外改变 MIDI 音符的绝对播放时间。
  - 根本原因：MIDI 音符保存为片段内相对 tick；如果片头缩短或延长只改片段起点和长度，不迁移内部音符相对 tick，用户听到的音符会整体偏移，和常见时间线修剪语义不一致。片头缩短还可能截掉左边界前的音符，不能静默删除或截断。
  - 采用的解决方式：扩展 `AppMidiClipActions`，新增 `trimMidiClipStartLaterOneBeat` 和 `extendMidiClipStartEarlierOneBeat`；片头缩短先拒绝会被截掉的音符，再把保留音符相对 tick 左移一拍；片头延长把已有音符相对 tick 右移一拍；两者都让音符绝对播放时间保持不变。JUCE 首屏新增“缩短片头”和“延长片头”按钮。2026-07-02 起，新增核心 `SetMidiClipStartKeepingNoteTimesCommand`，把片段左边界与内部音符相对 tick 迁移作为一次原子命令执行；CTest 覆盖成功缩短片头、成功延长片头、片头缩短音符边界拒绝、核心命令撤销/重做，以及应用层片头缩短/延长撤销/重做。
  - 后续规则：菜单、快捷键、AI 工具和拖拽左边界修剪必须明确“保持绝对时间”还是“移动内容”的语义；默认修剪语义应保持保留音符的绝对播放时间，不得静默删除、截断或移动用户已有音符。音频素材片头修剪需要独立素材偏移模型，不能直接套用 MIDI 音符迁移规则。任何同时修改片段时间和多个音符时间的动作，都必须使用一次原子命令或等价组合命令，不得把一个按钮动作拆成多次用户撤销；改变片段长度前后要按“缩短先移音符、延长先扩大片段”的顺序测试临时越界风险。
- 创建桌面默认 MIDI 音符增删入口时：
  - 触发场景：首屏已经能创建空 MIDI 片段后，需要让用户在片段里放入最小可听内容，并让时间线摘要显示音符数量。
  - 根本原因：如果 UI 直接调用 `Project::createMidiNote` 或 `Project::removeMidiNoteById`，会把目标片段校验、默认音高、默认长度、片段空间检查、空片段检查、dirty 状态和错误文案分散到界面层；后续快捷键、AI 工具和钢琴卷帘也会重复这些规则。
  - 采用的解决方式：新增 `AppMidiNoteActions`，只在目标片段存在、类型为 MIDI、且添加或删除前置条件满足时才进入写入路径；默认添加使用 C4 四分音符，删除入口只删除目标片段里时间位置最后的音符；`AppTimelineStatus` 行增加原始 `ClipType`，UI 选择 MIDI 片段时不再解析中文显示标签。2026-07-02 起，默认音符创建通过 `AddMidiNoteCommand`，末尾音符删除通过 `DeleteMidiNoteCommand` 进入 `AppProjectSession` 命令历史。CTest 覆盖成功添加、连续追加、缺失片段、音频片段拒绝、片段已满拒绝、删除末尾音符、空片段删除拒绝、缺失片段删除拒绝、音频片段删除拒绝，以及添加/删除撤销重做。
  - 后续规则：后续菜单、快捷键、AI 工具或钢琴卷帘创建和撤回默认音符时，应复用 `AppMidiNoteActions` 或等价应用层入口；显示文本不得承担逻辑判断；失败校验不得触碰 `editProject()`，避免把未修改工程标脏；创建和删除音符应继续通过核心命令进入会话历史。
- 复制桌面末尾 MIDI 音符时：
  - 触发场景：首屏已有默认音符追加、删除和基础微调后，需要让用户快速重复已有旋律片段，但仍没有钢琴卷帘、任意音符选择或剪贴板。
  - 根本原因：直接复制 `MidiNoteEvent` 容易复用源 note id，后续命令、播放调度和工程引用会把源音符与副本误认为同一对象；如果 UI 直接调用 `editProject().createMidiNote`，空片段、音频片段、缺失片段和空间不足失败也会误标 dirty。
  - 采用的解决方式：扩展 `AppMidiNoteActions`，新增 `duplicateLastMidiNoteInClip`，只复制时间位置最后的音符，把副本放在源音符结束 tick，保留长度、音高、力度和通道，并通过 `AddMidiNoteCommand` 分配新 id；先拒绝缺失片段、音频片段、空片段和副本超出片段右边界，再进入命令历史。JUCE 首屏新增“复制末尾音符”按钮。CTest 覆盖成功复制、新 id、属性保留、空间不足拒绝、空片段、缺失片段、音频片段拒绝和复制撤销重做。
  - 后续规则：在正式钢琴卷帘、剪贴板、快捷键或 AI 工具能选择任意音符前，首屏复制只允许作用于时间位置最后的音符；任何复制操作都必须生成新 note id，失败路径不得触碰 `editProject()`；复制成功必须能作为一次用户动作撤销/重做。
- 调整桌面末尾 MIDI 音符音高时：
  - 触发场景：首屏已经能追加和删除默认 MIDI 音符后，需要一个不依赖钢琴卷帘的最小旋律修改入口，让用户能把末尾音符升高或降低一个半音。
  - 根本原因：核心 `setMidiNotePitch` 只负责单个 note id 的合法性校验，不知道首屏当前没有任意音符选择 UI；如果 UI 直接寻找音符并调用核心，会把“末尾音符”选择规则、0-127 音高边界、空片段失败和 dirty 语义分散到界面层。
  - 采用的解决方式：扩展 `AppMidiNoteActions`，新增 `raiseLastMidiNotePitchInClip` 和 `lowerLastMidiNotePitchInClip`，沿用时间位置最后的音符选择规则，先拒绝缺失片段、音频片段、空片段和越界音高，再通过 `SetMidiNotePitchCommand` 进入 `AppProjectSession` 命令历史。JUCE 首屏新增“升高音符”和“降低音符”按钮。CTest 覆盖成功升高、成功降低、127 上界拒绝、0 下界拒绝、空片段拒绝和音高撤销/重做。
  - 后续规则：在正式钢琴卷帘、快捷键或 AI 工具能选择任意音符前，首屏音高调整只允许作用于时间位置最后的音符；任何音高修改都必须保留 MIDI 0-127 边界并通过核心命令执行，不得通过 velocity、channel 或显示文本间接判断目标音符。
- 调整桌面末尾 MIDI 音符力度时：
  - 触发场景：首屏已有末尾音符添加、删除和音高微调后，需要继续提供不依赖钢琴卷帘的最小力度修改入口。
  - 根本原因：核心 `setMidiNoteVelocity` 能校验单个 note id，但 MIDI velocity 0 通常表示 Note Off，不能作为当前工程保存的发声音符力度；如果 UI 直接加减 velocity，容易把 1-127 边界、末尾音符选择、空片段失败和 dirty 语义分散。
  - 采用的解决方式：扩展 `AppMidiNoteActions`，新增 `increaseLastMidiNoteVelocityInClip` 和 `decreaseLastMidiNoteVelocityInClip`，按 8 步进调整时间位置最后的音符力度，先拒绝缺失片段、音频片段、空片段和越界力度，再通过 `SetMidiNoteVelocityCommand` 进入 `AppProjectSession` 命令历史。JUCE 首屏新增“增强力度”和“减弱力度”按钮。CTest 覆盖成功增强、成功减弱、127 上界拒绝、1 下界拒绝、空片段、缺失片段、音频片段拒绝和力度撤销/重做。
  - 后续规则：在正式钢琴卷帘、快捷键或 AI 工具能选择任意音符前，首屏力度调整只允许作用于时间位置最后的音符；保存的发声音符 velocity 必须保持在 1-127，成功修改必须通过核心命令进入历史，失败路径不得触碰 `editProject()`。
- 调整桌面末尾 MIDI 音符长度时：
  - 触发场景：首屏已有末尾音符音高和力度微调后，需要继续补齐最小音符时值修改能力，但仍不能假装已经有钢琴卷帘或任意音符选择。
  - 根本原因：核心 `setMidiNoteTiming` 会验证音符时间，但 `AppProjectSession::editProject()` 会先标脏；如果 UI 直接调用核心，缩短到无效长度、延长越过片段右边界、空片段和音频片段失败都会把未变工程误标 dirty。
  - 采用的解决方式：扩展 `AppMidiNoteActions`，新增 `lengthenLastMidiNoteInClip` 和 `shortenLastMidiNoteInClip`，按十六分音符 tick 调整时间位置最后的音符长度，先拒绝缺失片段、音频片段、空片段、短于最小长度和越过片段右边界，再通过 `SetMidiNoteTimingCommand` 进入 `AppProjectSession` 命令历史。JUCE 首屏新增“延长音符”和“缩短音符”按钮。CTest 覆盖成功延长、成功缩短、右边界拒绝、最短长度拒绝、空片段、缺失片段、音频片段拒绝和长度撤销/重做。
  - 后续规则：在正式钢琴卷帘、快捷键或 AI 工具能选择任意音符前，首屏长度调整只允许作用于时间位置最后的音符；长度微调不移动音符起点，不得越过片段边界，成功修改必须通过核心命令进入历史，失败路径不得触碰 `editProject()`。
- 移动桌面末尾 MIDI 音符起点时：
  - 触发场景：首屏已有末尾音符长度微调后，需要继续补齐最小位置移动能力，但仍没有钢琴卷帘、任意音符选择或拖拽编辑。
  - 根本原因：起点移动和长度修改共用 `setMidiNoteTiming`，同样存在失败路径误标 dirty 的风险；同时原音符编辑按钮已经占满一行，继续横向追加按钮会让桌面首屏布局变得拥挤。
  - 采用的解决方式：扩展 `AppMidiNoteActions`，新增 `moveLastMidiNoteStartEarlierInClip` 和 `moveLastMidiNoteStartLaterInClip`，按十六分音符 tick 移动时间位置最后的音符起点，保持长度、音高、力度和通道不变；先拒绝缺失片段、音频片段、空片段、起点早于 0 和右边界超出片段，再通过 `SetMidiNoteTimingCommand` 进入 `AppProjectSession` 命令历史。JUCE 首屏新增“左移音符”和“右移音符”，并把音符编辑拆成音高/力度行与长度/位置行。CTest 覆盖成功左移、成功右移、左右边界拒绝、空片段、缺失片段、音频片段拒绝和起点撤销/重做。
  - 后续规则：在正式钢琴卷帘、快捷键或 AI 工具能选择任意音符前，首屏起点移动只允许作用于时间位置最后的音符；位置微调必须保留音符长度和其他 MIDI 属性，成功修改必须通过核心命令进入历史，失败路径不得触碰 `editProject()`；新增首屏按钮前要检查行宽和窗口高度，必要时拆行并调整默认窗口尺寸，而不是继续横向堆叠。
- 整理需求与规划文档时：
  - 触发场景：阶段性开发累计产生 `docs/superpowers/specs/` 和 `docs/superpowers/plans/` 下 93 个需求与计划草稿，后续判断容易分不清哪份文档是当前基线。
  - 根本原因：早期按阶段写设计和计划有利于执行，但长期保留全部草稿会让需求来源分散，并与用户希望“后续所有需求与规划都在一份最终文档上更改”的要求冲突。
  - 采用的解决方式：新增 `docs/TrackLoom-最终需求与规划.md` 作为唯一需求与规划基线，吸收旧草稿中仍有价值的产品目标、架构边界、已完成基础、阶段路线和未定决策，并删除旧的分散草稿目录。
  - 后续规则：普通需求、规划、阶段路线和验收标准只更新最终文档；`AGENTS.md` 只补充协作规则和经过验证的经验教训，不再承担完整需求规格职责。
- 管理协作文档版本控制时：
  - 触发场景：`AGENTS.md` 和最终需求文档已经成为长期开发依据，但 `.gitignore` 仍把它们当作本地草稿忽略，导致代码提交和文档基线不同步。
  - 根本原因：早期忽略本地规划文档能避免临时草稿污染仓库；在需求已经收敛成唯一基线后，继续忽略会让关键决策、验收标准和经验教训缺少版本历史。
  - 采用的解决方式：移除 `.gitignore` 中对 `AGENTS.md` 和 `docs/` 的忽略，只保留构建产物忽略，并把最终需求文档和协作规则纳入 Git。
  - 后续规则：长期有效的协作规则和唯一需求基线必须随代码一起版本控制；临时实验记录、本机设置、密钥、构建目录和无用草稿仍不得提交。
- 接入桌面全局命令入口时：
  - 触发场景：基础命令面板已有会话状态后，需要把“打开命令面板”接入菜单和快捷键，避免后续可见弹窗绕过已有命令系统。
  - 根本原因：如果 JUCE 菜单、快捷键、命令面板和后续 AI 工具分别维护命令编号与回调，命令可见性、快捷键标签、disabled 状态和执行结果会逐步分叉。
  - 采用的解决方式：新增稳定 `AppMainMenuCommand::OpenCommandPalette`，同步映射到 `AppCommandKind`、`AppCommandHandlers`、`AppCommandShortcuts`、工具菜单和 JUCE 处理函数；打开时构建带快捷键标签的 `AppCommandPaletteSession`，CTest 覆盖菜单快照、命令面板展开、快捷键标签、分发器和 `Ctrl+K`/`Ctrl+Shift+P` 映射。
  - 后续规则：新增桌面全局命令时，应优先从 `AppMainMenuCommand` 建立稳定 id，再同步分发器、快捷键、命令面板标签和 JUCE 回调测试；UI 只渲染和触发应用层快照，不应自行复制命令映射。
- 接入可见命令面板时：
  - 触发场景：命令面板已有打开命令和会话状态后，需要在 JUCE 桌面壳中显示搜索框、结果行和高亮状态。
  - 根本原因：如果可见 UI 自己拼接行文本、解释 disabled 状态或直接执行命令，后续菜单、快捷键、AI 工具和诊断面板会出现多套命令展示与执行规则。
  - 采用的解决方式：在 `AppCommandPaletteSession` 中新增 `describeAppCommandPaletteSessionRow`，统一生成“菜单组 / 命令名 / 快捷键 / 不可用”行文本；JUCE 浮层只渲染 session view，通过 `AppCommandDispatcher` 执行当前高亮命令，并用 CTest 覆盖行文本格式、完整 Debug 构建和完整 CTest。
  - 后续规则：命令面板 UI 扩展鼠标点击、滚动、图标、自定义快捷键或 AI 命令时，必须继续复用 `AppCommandPaletteSession` 和 `AppCommandDispatcher`；UI 不得重复实现命令过滤、disabled 判断、行文本拼接或执行分发。
- 接入命令面板鼠标行激活时：
  - 触发场景：可见命令面板已经能用 Enter 执行高亮命令后，需要让用户直接点击结果行执行命令。
  - 根本原因：JUCE 可见行只是当前过滤结果的窗口切片；如果 UI 直接按行号或旧 command id 执行，搜索文本变化、面板关闭、disabled 状态或可见窗口偏移都可能让点击路径绕过应用层校验。
  - 采用的解决方式：新增 `activateAppCommandPaletteSessionRow`，UI 只传当前行绑定的 command id；应用层重新确认会话打开、命令仍在当前过滤结果里且 enabled，再复用 `AppCommandDispatcher` 执行；JUCE 行标签只负责把鼠标事件转成该应用层调用。
  - 后续规则：命令面板的鼠标、触控、滚动窗口和未来图标行都不得直接执行可见行缓存；所有点击激活必须通过 `AppCommandPaletteSession` 按当前过滤结果二次校验，并覆盖关闭、disabled、缺失行和缺失 handler 测试。
- 接入命令面板分页导航时：
  - 触发场景：可见命令面板只显示 6 行结果后，需要让用户用 PageUp/PageDown 快速跨页移动高亮，并让列表窗口跟随高亮位置。
  - 根本原因：如果可见窗口首行计算留在 JUCE 私有函数里，分页、鼠标滚动、触控滚动和后续诊断视图会复制不同的窗口规则；如果分页直接按行号跳转，还可能落到 disabled 命令或越过列表边界。
  - 采用的解决方式：在 `AppCommandPaletteSession` 中新增分页高亮移动，按可见行数跳转并把结果夹到第一/最后 enabled 命令，遇到 disabled 目标行时按翻页方向寻找最近 enabled 命令；同时新增 `firstVisibleAppCommandPaletteSessionRowIndex` 作为纯应用层 helper，JUCE 只读取 helper 结果渲染窗口。CTest 覆盖分页移动、边界夹紧、disabled 目标跳过、disabled-only 查询和零可见行数。
  - 后续规则：命令面板滚轮、触控滚动、更多可见行布局或虚拟列表都必须复用应用层可见窗口计算或等价测试边界；高亮不得落到 disabled 命令，分页和滚动不得把窗口规则隐藏在单一 UI 实现里。
- 接入命令面板 Home/End 边界导航时：
  - 触发场景：命令面板已有上下键和 PageUp/PageDown 后，需要支持常见列表键盘习惯，用 Home/End 快速到达第一条或最后一条可执行命令。
  - 根本原因：Home/End 如果在 JUCE 层直接操作行号，容易把 disabled 跳过、closed session 和 disabled-only 查询的规则复制到 UI；如果把 Home/End 当作执行命令，也会混淆“移动高亮”和“确认执行”。
  - 采用的解决方式：在 `AppCommandPaletteSession` 中新增 `moveHighlightToFirst` 和 `moveHighlightToLast`，只把高亮移动到第一/最后 enabled 命令，closed、空结果和 disabled-only 查询保持无高亮；JUCE 只绑定 `homeKey`/`endKey` 并刷新面板。CTest 覆盖首尾移动、可见窗口跟随、disabled-only 查询和 closed session。
  - 后续规则：命令面板新增键盘导航时，必须先在会话层定义“移动高亮但不执行”的语义，并测试 disabled、空结果和关闭状态；JUCE 不得自行复制高亮边界规则。
- 抽出命令面板可见行快照时：
  - 触发场景：命令面板已有分页、Home/End 和鼠标点击后，JUCE 刷新函数仍在按 `firstVisibleRow + visibleIndex` 自行切片完整 rows，并维护可见行到 command id 的绑定。
  - 根本原因：如果 UI 层保留切片、总数和上下是否还有隐藏行的判断，后续鼠标滚轮、范围提示或虚拟列表容易复制出不同规则，点击行缓存也会脱离应用层测试边界。
  - 采用的解决方式：新增 `AppCommandPaletteVisibleRowsView` 和 `describeVisibleAppCommandPaletteSessionRows`，统一返回 `firstRowIndex`、`totalRowCount`、`hasPreviousRows`、`hasNextRows` 和裁剪后的 `rows`；JUCE 只渲染该快照并绑定当前可见行 command id。CTest 覆盖跨页切片、末页、closed、零可见行和 disabled-only 查询。
  - 后续规则：命令面板滚轮、范围提示、虚拟列表或可见行点击扩展必须消费应用层可见行快照；UI 不得重新按完整 rows 自行切片或推断滚动提示。
- 接入命令面板范围提示时：
  - 触发场景：可见行快照已经能返回首行、总数和裁剪 rows 后，需要在命令面板标题区显示当前正在查看的结果范围。
  - 根本原因：范围提示如果由 JUCE 根据标签数量临时拼接，会再次复制一套 one-based 索引、单行格式、空窗口处理和总数规则；后续滚轮或诊断视图容易显示不同结果。
  - 采用的解决方式：新增 `describeAppCommandPaletteVisibleRowsRange`，从 `AppCommandPaletteVisibleRowsView` 生成 `"3-8 / 12"`、`"1 / 1"` 或空字符串；JUCE 只新增一个右对齐 label 显示该文本。CTest 覆盖中间页、末页、单行 disabled-only 和零可见行。
  - 后续规则：命令面板里所有结果范围、滚动提示、诊断摘要都必须复用应用层范围描述或等价测试边界；UI 层不得自行把 row index 转成人类可读范围。
- 接入命令面板鼠标滚轮步进时：
  - 触发场景：命令面板已经有可见行快照和范围提示后，需要让鼠标滚轮可以浏览较长命令结果。
  - 根本原因：如果 JUCE 直接移动可见行或保存独立滚动偏移，会绕开 `AppCommandPaletteSession` 的 disabled 跳过、边界夹紧、范围提示和点击二次校验规则；如果沿用上下键循环语义，滚到底部会突然回到顶部，不符合滚轮预期。
  - 采用的解决方式：新增 `moveHighlightByWheelSteps`，滚轮正数向下、负数向上，跳过 disabled，顶部/底部夹住不循环；JUCE 只把 `MouseWheelDetails::deltaY` 转成 `+1/-1` 步数，结果行也把 wheel 事件转交同一入口。CTest 覆盖步进、跳过 disabled、上下边界夹紧、零步数、disabled-only 和 closed session。
  - 后续规则：命令面板滚轮、触控板和后续独立滚动条都必须先明确是否移动高亮、是否拥有独立滚动偏移；该底层能力只是滚轮驱动高亮步进并让窗口跟随，不得宣称已经支持惯性触控板累计或独立列表滚动位置。
- 接入选择依赖的菜单命令时：
  - 触发场景：基础菜单和命令面板已经能展示静态命令后，需要把当前目标轨道、目标片段对应的编辑动作放进主菜单和命令面板。
  - 根本原因：如果 JUCE 菜单自己判断选择状态，命令面板、快捷键和分发器会逐步形成多套 enabled 规则；如果分发器只新增 command id 而不补 handler 测试，菜单可见命令可能点击后静默失败。
  - 采用的解决方式：扩展 `AppMainMenuSelection`，由桌面壳把当前目标乐器轨、目标音频轨、目标 MIDI 片段和目标音频片段传给 `describeAppMainMenu`；菜单快照统一判断轨道类型、片段类型、移动边界和跨轨目标合法性；`AppCommandDispatcher` 同步新增稳定 `AppCommandKind` 与 handler，并用 CTest 覆盖无选择禁用、有效选择启用、同轨目标禁用、类型错配禁用和每个 handler 映射。
  - 后续规则：新增依赖当前选择的菜单、命令面板、快捷键或 AI 工具命令时，必须先定义应用层选择输入和 enabled 规则，再同步命令 id、分发器、JUCE handler 和测试；UI 不得绕过应用层菜单快照自行启用或执行命令。
- 建立自定义快捷键映射基础时：
  - 触发场景：默认快捷键已用于键盘触发和命令面板标签后，需要允许未来设置界面覆盖绑定。
  - 根本原因：如果设置界面直接修改快捷键表，旧默认绑定、命令面板标签和冲突处理会分散；冲突覆盖如果静默生效，会让同一组合键指向两个命令或错误抢占原命令。
  - 采用的解决方式：新增 `customizeAppShortcutBindings`，从默认绑定生成活动表；非冲突覆盖会移除该命令旧绑定并加入新 chord；冲突覆盖记录 `AppShortcutConflict` 并保留原绑定；`appCommandIdForShortcut(chord, bindings)` 让后续 UI/设置能读取同一活动表。CTest 覆盖替换默认、冲突拒绝、无默认命令新增绑定。
  - 后续规则：未来快捷键设置 UI、命令面板标签和持久化加载都必须使用同一活动绑定表；冲突不得静默覆盖，用户确认或修正前应保留旧绑定。
